# REVIEWS: T5 户外地垫市场数据 -> SQLite 转换工程

## 20260923 SPEC 3.5 BSR平均三档与字段契约（当前待重建）

用户进一步确认：父体月度主销量和主销售额直接使用同一月份+父ASIN组内Q列“月销量”和T列“月销售额($)”的有效值平均；BSR在独立支线内对同月同父ASIN的有效排名取平均，再按头部/中部/尾部三档划分。BSR平均只决定分层，不改写Q/T主指标。Q/T空白、不可解析和负数不纳入，明确0纳入；有效行数、覆盖率、最小值、最大值和`AVERAGE_CONFLICT`必须保留。子体销量和子体销售额仍保留为诊断，不再替代Q/T主指标。

已更新 `docs/SPEC.md` 和人工流程文档。当前3.1/3.2/3.3/3.4的Excel、HTML、JSON和Pages数字仍然是旧模型，必须按3.5改造程序后重新构建和审计，才能生成新的当前结果。

## 20260923 SPEC 3.2口径纠正（历史）

用户纠正了两处当前规范：第一，`MOM`定义为本月与去年同月比较，不能使用上一自然月；`YOY`定义为本年与上一年比较，年度计算仍只使用双方共同覆盖月份。第二，`小类目`中的 `Outdoor Rugs` 文本不参与候选池筛选；候选池只依据“小类BSR”是否存在1—100（含边界）的有效排名。多值小类BSR保留全部解析值并标记`MULTI_BSR`，行级BSR使用有效1—100排名的中位数，最小排名仅作诊断。

因此下方原“20260922 SPEC 3.1完整审计”记录只代表旧规则下的历史交付，45,935候选行、1,272父体月份、2025.08的354条歧义排除和旧MOM数值均不得继续作为3.2结果。必须按 `docs/SPEC.md` 3.2重建模型、Excel、HTML、JSON、测试和GitHub Pages后，才能形成新的当前审计记录。

## 20260922 SPEC 3.1完整审计（历史记录，待3.2替代）

当前唯一主源为 `data/raw/0920new` 的24个按月文件，覆盖2024.08—2026.07，共48,000条原始行。审计重新读取每个源文件，核对文件月份、业务Sheet、65列表头及顺序，并记录SHA-256。旧总表、旧参考表和SPEC 3.0以前结果没有进入当前计算。

小类范围已从“任意小类最小排名”修正为“小类目与小类BSR按换行位置逐项对应，只取Outdoor Rugs排名”。纳入1—100（含100）后为45,935条候选子体行。2025.08有354行只有一个Outdoor Rugs名称但两个排名，无法可靠对应，已标记 `AMBIGUOUS_CATEGORY_RANK` 并排除；月度MOM、YOY和年度共同月份中均显示范围提示。当前同月ASIN重复0组。

父体层按月份+父ASIN聚合，共1,272个父体月份组合；父ASIN缺失依次回退ASIN和源行ID。父体月销量采用：一致值→一致；严格多数>50%→众数；其他包括50%平局→中位数并标 `MEDIAN_CONFLICT`；无有效值留空。当前681组出现多个源销量值，243组实际使用中位数，全部进入或可在07页诊断。BSR使用候选子体Outdoor Rugs排名中位数，修复了20.5、5.5等小数落档问题。

有效子体销售额只累加同时具有非负子体销量和子体销售额、且不属于0销量/正金额矛盾的行；明确0/0保留。父体没有有效行时金额和均价留空，状态NONE；当前356个父体月份无有效子体金额。有效子体加权均价已经修正为有效子体销售额÷有效子体销量，不能除以父体估计销量。源“月销售额($)”只用于回勾及隐含价格诊断，不在父体下累加。

PP按父体候选子体标题中完整单词plastic的多数归类，高客单价为补集；当前92个父体月份存在混合标题并显示提示。整体父体数、父体销量和有效子体金额逐月均等于PP+高客单价。Genimo作为品牌视角，分别计算整体份额和PP内份额。MOM为本月对上月，YOY为本月对去年同月；年度仅使用相同月份集合，2025对2024为8—12月，2026对2025为1—7月。

当前工作簿包含16个子表，形成90原始输入→91候选子体→92父体月度→业务月度/年度/分层/策略/勾稽的公式链。`npm run test:parent`独立重新扫描源文件并核对Excel、JSON和HTML，结果PASS；378,821个保存公式由独立解释器实际求值，差异0。输入扰动验证了单行子体金额+100、全部子体字段清空和明确0三种情形。抽样 `2026.07 / B0BM74444Q`：69条候选行、销量众数3,901出现39次、父体估计销量3,901；29条有效子体、销量3,100、销售额154,430、加权均价49.816129、覆盖率42.028986%，与工作簿一致。

详细步骤见 `docs/数据处理流程与人工验算-20260922.md`，机器摘要见 `outputs/20260920-new-source-parent-model/完整审计结果.json`。本记录证明源读取、规则实现、公式计算和交付文件内部一致；不证明供应方估算等于真实成交，也不等同于桌面Excel整簿重算。完整簿约15MB，Artifact Tool完整导入耗时过长，代表页裁剪预览和网页交互验收须作为独立证据记录。

代表页视觉预览已覆盖00数据总览、01整体市场月度、08策略、92父体汇总、94规则说明；表头、格式、空白状态和示例值可读。浏览器实测四部分下拉导航、31个小节的单独/全部展开收起、年份/月份控件、父ASIN检索和24张交互图；搜索 `B0BM74444Q` 只返回1个父体，并显示3,901、154,430、42.0%和49.82。

业务分支交付提交 `20cad7c1082e7500272079ed9d612fc9675f7c02` 已推送到 `codex/formula-xlsx-20260909`。Pages提交 `840f5c29e46cb2bfb300de29b60ef15bb3b2edde` 已推送到 `gh-pages`；线上地址 `https://thezhouverse.github.io/t5-outdoor-rug-market-analysis/` HTTP 200，回读含当前批次、45,935候选行、1,272父体月份组合和严格多数规则。线上下载XLSX为15,350,134字节，SHA-256 `09111ef66075da0a336b01a8ac6b0806707b629434b0ea4d89e02a34dbb282ea`，与本地交付逐字节一致。

下方20260920 SPEC 3.0以及更早记录已被本节替代，其中46,780候选行、1,334父体组合、≥50%众数和旧金额空值处理均不得用于当前交付。

## 20260920 新源父体口径执行记录（SPEC3.0）

本批次使用 `data/raw/0920new` 的 24 个按月文件，覆盖 2024.08—2026.07。文件与业务 Sheet 月份逐一一致，合计 48,000 条原始行；先筛小类 BSR 1—100（含100）后得到 46,780 条候选子体行，形成 1,334 个月份+父ASIN父体组。同月同一 ASIN 重复组为 0。

本批次采用两层模型：91页保留全部候选子体明细；92页按月份+父ASIN分别聚合父体月销量和有效子体销售额。父体月销量不按最小BSR选代表，众数覆盖率达到50%取众数，否则取中位数并标记 `MEDIAN_CONFLICT`。有效子体销售额只累加同时存在子体销量和子体销售额的子体行；源字段 `月销售额($)`不在父体下相加。

当前模型结果：父体月销量冲突组 192 个；2026.07 有 62 个父体、1,984 条 BSR候选子体行，父体月销量 226,900.5，有效子体销售额 $4,302,980，子体有效覆盖率 37.85%，月销量冲突父体 9 个。2025.12、2026.01、2026.02 的子体覆盖率分别为 4.13%、4.72%、5.76%，这些月份的销售额只能称为有效子体销售额，不能解释为完整市场销售额。

抽样父体 `2026.07 / B0BM74444Q`：候选子体 69 行，月销量众数 3,901、出现39次、覆盖率56.52%，中位数3,901，最小BSR行月销量6,167；有效子体29行，子体销量合计3,100，子体销售额合计$154,430，覆盖率42.03%。该样本证明最小BSR行不能代表父体月销量。

已生成并通过结构构建：`outputs/20260920-new-source-parent-model/户外地垫市场分析-SPEC3-父体口径可回勾.xlsx`、`户外地垫市场分析-SPEC3-父体口径.html`、`户外地垫市场分析-SPEC3-父体口径.json`、`户外地垫市场分析-SPEC3-父体口径.md`和`构建清单.json`。工作簿保留 90 原始输入、91 BSR候选子体明细、92 父体月度汇总、业务月度页、分层、冲突诊断和规则说明；HTML与JSON由同一模型生成。

当前未宣称桌面Excel整簿重算或GitHub推送通过；下一步人工打开工作簿，抽查低覆盖月份和冲突父体，再决定提交与推送。

## 当前口径：BSR筛选后保留全部候选行（2026-09-14）

用户已将统计单位改为候选源行：每月先筛小类BSR 1—100（含100），再保留每条符合条件的源行，不按父ASIN去重，也不选择代表行。父ASIN、ASIN和源行号只用于回勾与重复结构审计。PP按候选行商品标题完整单词 `plastic` 匹配，高客单价是PP补集；整体=PP+高客单价；GENIMO按同一候选行池的品牌筛选，GENIMO PP为品牌与PP交集。

本次当前批次为 `spec21-62ffd074bca5`，主源SHA-256为 `62ffd074bca5299917d529bc033fdd01b9747ed6422d01177afb2b0c9b8a5701`。主源读取73,810行，BSR候选9,973行，候选父ASIN诊断键1,101，缺失父ASIN候选0行。2025.09为114条候选行（PP 65、高客单价49）；2026.07为1,307条候选行（PP 879、高客单价428），整体数量、销量、销售额均逐项回加一致。

前端与候选行版XLSX已重新对齐：HTML的`report-data`嵌入与交付JSON逐字对应同一批次数据；可见月份统一使用零填充`YYYY.MM`，月度回勾列使用“BSR候选行数（回勾）”和“截去数”，不再显示会误导的“候选父ASIN数/重复父ASIN行数”。

Pages已同步当前前端：`gh-pages`提交为`0197ca05ea9b2b64cd4e3f25a687bac1050978fa`，发布地址为 https://thezhouverse.github.io/t5-outdoor-rug-market-analysis/；线上HTTP状态200，页面正文包含当前批次`spec21-62ffd074bca5`、`report-data`、`2026.07`和“BSR候选行数（回勾）”，未发现旧的父ASIN误标表头。线上发布副本与本地优化版HTML内容标识一致。

本次源文件指纹与上一批次不同，但通过逐月/源行/字段对照确认，业务字段没有变化：两版XML均为73,811条含尾部说明行的结构记录，源行键集合一致，ASIN、父ASIN、标题、品牌、BSR、销量、销售额和价格逐项差异为0。当前解析器按既有规则排除1条非业务尾部说明行，因此业务原始行仍为73,810条。
对照明细已保存为 `docs/audit/source-semantic-comparison-20260914.json`，可与当前源指纹和审计摘要互相核对。

2026.07当前候选行池：整体销量5,000,952、销售额$313,341,314；PP销量3,920,991、销售额$228,785,747；高客单价销量1,079,961、销售额$84,555,567；GENIMO候选行销量2,042,996、销售额$131,825,522；GENIMO PP候选行销量1,849,585、销售额$115,743,702。因2026.07候选行数和上一年差异很大，报告标记“可比性受限”，这些增幅不能直接解释为真实市场增长。

独立源XML字段核对1,109,650项、公式缓存/跨文件检查1,044,098项，输入扰动（单行移出BSR、同父ASIN其他行独立计入、空值/真实零、规划分配）均通过，错误0。当前可验证的工作簿为 `outputs/20260911-spec2-market-analysis/户外地垫市场分析-SPEC2-候选行版.xlsx`；原规范文件名当时被WPS占用，未强制关闭用户文件。桌面Excel整簿重算和远端推送尚未完成。

## 口径纠偏：BSR先筛、父ASIN后去重（历史版本）

用户已明确最终主池顺序：原始月度行先筛小类BSR 1—100（含100），再按父ASIN去重并选代表，之后才按代表标题拆PP与高客单价；整体市场必须是同一主池中PP与高客单价的并集；GENIMO也必须从同一主池中按品牌筛选，GENIMO PP为品牌与PP交集。所有销量、销售额、均价、MOM、YOY、分层、进留退和2027规划都只能使用这套主池。

此前本地工作簿和下方“SPEC 2.1完整修复”按“全量去重整体盘 + 独立BSR范围”双分支生成。它们的公式审计仍然有效地说明旧分支内部计算一致，但不满足本次确认的新统计顺序；在按新主池重建前，不能作为当前业务结果或验收数字。旧批次中的“整体盘407”“整体销量333,653”等数字全部标记为待重建，不能继续引用。

### 新口径已完成任务

- 模型入口已从 `fullDedup = dedup(raw.rows, flags)` 改为 `BSR候选行 → 父ASIN去重` 的唯一业务主池。
- PP、高客单价、整体、GENIMO和GENIMO PP全部从该主池取数；删除全量整体盘分支和独立Top100回勾分支。分类改为筛选去重后的代表标题；旧版家族任一成员命中会使当前候选池中8个父ASIN分类不同。
- 已重建XLSX 90→91→92→01—08、HTML、JSON、Markdown和构建清单，并完成2025.09、2026.07及全部公式/跨文件验收。
- 月度MOM在当前表头和说明中明确为本月对去年同月；年度YOY使用相邻年份共同覆盖月份。若业务后续改为自然月环比，需作为新口径重新构建。

本地验收结果：原始业务行73,810，BSR候选原始行9,973，父ASIN主池5,190个Listing月次；公式393,073个、保存回读检查742,698项、输入扰动440项、独立源审计1,109,650项，错误均为0。2026.07整体主池111个父ASIN、销量246,611、销售额$11,827,794；PP为65/167,240/$6,022,233，高客单价为46/79,371/$5,805,561，三者逐项回加一致。

### 父ASIN不去重的影响复核（解释当前口径，不改变当前结果）

原始读取本身已通过独立 XLSX XML 对照；当前差异主要来自源表的变体展开。以同一套“先筛 BSR 1—100”候选为基准，保留每条候选行会把同一父ASIN的多个子体重复计入：

| 范围 | BSR候选行数 | 父ASIN去重数 | 不去重销量 | 去重销量 | 销量高估 | 不去重销售额 | 去重销售额 | 销售额高估 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 2025.09 | 114 | 96 | 158,412 | 131,290 | +20.66% | $6,319,633 | $5,112,406 | +23.61% |
| 2025H1 | 1,145 | 656 | 1,249,923 | 1,062,942 | +17.59% | $70,867,039 | $58,608,296 | +20.92% |
| 2026H1 | 3,638 | 664 | 15,111,465 | 1,206,174 | +1,152.84% | $1,006,424,221 | $65,623,372 | +1,433.64% |
| 2026.07 | 1,307 | 111 | 5,000,952 | 246,611 | +1,927.87% | $313,341,314 | $11,827,794 | +2,549.19% |

源行例证：2026.07 父ASIN `B0DPMBVBKT` 有35条 BSR合格子体行，`月销量`均为12,342；按当前“不去重”规则仅这一父ASIN就会计成431,970件。2025.05 父ASIN `B0BFMNL9FK` 有155条候选行，大量子体行的 BSR 和月销量重复。这说明当前指标是“候选源行口径”，不能直接解释成父体级独立商品数；若业务以后要父体级市场规模，必须另行确认父体汇总字段和代表规则，不能在当前结果上混用两套口径。

去重本身还必须配套“代表行”规则。2026.07 的111个父ASIN若取当前规则（最小有效BSR→销量/金额完整→价格完整→源行号）为246,611件/$11,827,794；若取首行为241,160件/$10,096,628，取最大销量为250,255件/$14,048,443，取最大销售额为246,068件/$29,578,230。因而参考表若采用了另一种代表行规则，仍会与当前结果不一致；这不是原始 XLSX 读取错误，而是统计口径需要明确的独立决策。

## SPEC 2.1 完整修复与交付审查（旧口径历史证据）

本节是旧口径的修复证据；下方2.0条目保留作历史追溯。正式主源未改，指纹为 `9ced3b100ece7d441b45ce8a1bd0eb9d3470854a832f071b02ecc2c6f91f5fac`，覆盖2022.06—2026.07共50个月、73,810条业务行。新主池结果已在上方“新口径已完成任务”登记，本节旧数字仍不作为当前结论。

### 已完成

- SPEC 2.1已重写并归档2.0；四个业务部分明确为整体、PP、高客单价补集、GENIMO品牌。GENIMO不再作为第三个市场参与整体加总。
- 源解析独立从XLSX XML核对1,109,650项，富文本、说明行、负BSR、小数BSR、100边界和2025.09回归均通过；2025.09为114条BSR候选行、96个去重Listing、逐行标题plastic命中65条。
- 代表行改为数值源行号决胜，修复字符串排序；274个Listing月次换回规则指定代表。BSR 1—100（含100）筛选后全部保留，取消额外最多100条截断，36个月恢复399个合格Listing月次。主源文件只读，未改写源值。
- XLSX现在是14张表的完整公式链：90→91→92→业务结果→93终端观察。实际求值2,196,411个公式，与独立模型1,911,423项期望值比较，差异0；8类输入联动情景440项断言通过；8个函数兼容案例通过。93校验不向业务公式提供输入。
- 最新跨文件回读保存2,196,411个公式，4,111,484项检查，错误0；JSON、Markdown、HTML、XLSX和构建清单共享同一批次元数据。HTML实际浏览器检查通过：5组导航、31个可折叠小节、26个交互图表、关闭/重新打开行为和读数均正常。14张表做了保存值/格式预览，年度比较列、下方表头和长标题已修正。

### 当前可回勾结果

| 项目 | 当前结果 |
|---|---:|
| 2026.07整体盘 Listing月次 | 407 |
| 2026.07整体盘销量 / 销售额 | 333,653 / $17,049,975 |
| 2026.07整体BSR 1—100 Listing | 111 |
| 2026.07 GENIMO PP销量 / 销售额 | 43,691 / $1,047,486 |
| 2026.07整体销量、销售额MOM | -12.2852% / -21.8908% |
| 2026.07 GENIMO PP销量、销售额MOM | -2.1369% / -52.0530% |

这些数字与修复前不同，原因是代表行选择和BSR范围保留规则改变，不是对源文件的修改。2026.07整体407对2025.07的1,783个Listing，已标“可比性受限”；它说明快照样本结构变化，不能单独当作全亚马逊真实市场结论。

### 验收边界

独立公式引擎覆盖当前工作簿实际使用的Excel函数，Artifact Tool仅做兼容性和预览检查；本机没有运行桌面Excel的整簿重算，不能宣称通过桌面Excel验收。超过200万条公式可能使首次打开和重算较慢。源为卖家精灵估算，程序能证明规则、公式、回勾和文件一致性，不能证明供应方估算等于真实成交。

本轮文件已在本地生成并写入构建清单；本记录不把本地构建当作GitHub推送或Pages上线证据。

## SPEC 2.0 回勾修复（2026-09-11）

- 针对 2025.09 “BSR前100为96、人工筛选看到65”的差异，直接读取主源 `2025.9` 子表复核：小类BSR 1—100（含100）为 114 条原始候选行，按父ASIN优先的月度 Listing 去重后为 96 个；PP按Listing组归类为 66 条原始候选行/50 个Listing，非PP为 48 条/46 个Listing。源表若同时保留标题 `plastic` 筛选，则 65 条是逐行PP命中数，不是整体市场数。
- 结果 HTML 的 1.1/1.2 表和可回勾 XLSX 月度表现在同时显示“BSR前100原始候选行数”和“BSR前100去重Listing数”；90页新增原始小类BSR、解析值和解析状态，便于按源行复核。
- Top100实现已调整为先在原始明细筛 BSR，再在候选范围内按月、Listing键去重和稳定截取；解析器不再把非整数小数（如 `1.5`）拆成伪排名。回归复核命令：`node tests/spec2_audit.mjs`，2025.09 结果固定为 114/96，标题逐行 `plastic` 命中为 65。

## 公式版结果工作簿（2026-09-09）— Round 13 结果缓存与参考模板复核

### 交付与验证

- 旧版工作簿把 GENIMO 与 PP/非PP并列成第三类，且没有把原始需求的每个分析项逐表对应；本轮已重建为一份 `outputs/20260909-formula-market-analysis/户外地垫市场分析-公式版-20260909.xlsx`。
- 现在的市场分区只有两块：整体市场=PP管+非PP高客单，非PP高客单是排除PP后的补集；GENIMO是整体市场中的品牌视角，不进入分类加总。
- 20 个工作表逐一对应原始要求：01/02/03整体市场，04/05/06 PP，07/08/09非PP，10/11/12 GENIMO品牌份额/进退层/2027规划（含链接数、库存、广告、利润及晋级/退出门槛），13周报汇总，14利润测试，15经营计划与实际，90明细输入，91聚合输入，92校验，93来源与规则，00总览与趋势判断。
- 工作簿覆盖 2025.01-2026.07 月度展示，2024 月份保留在聚合输入用于 YOY 基准；核心周期为 2026.01-06，2026.07 为 94 父体展示样本。粗分层为1-20/21-50/51-100，细分层为1-5/6-10/11-20/21-50/51-100。
- 结果页全部保留公式和缓存值：共 63,130 条明细、1,763 条聚合、9,479 个公式。公式缓存现在显式写成数值类型，15 页经营计划的 H1 合计、H1 达成率、7 月差额和执行状态在未打开 Excel 重算时也有可读结果。XLSX 回读确认 20 个工作表、无 `#REF!/#DIV/0!/#VALUE!/#NAME?/#N/A` 缓存错误。

### 参考模板对齐复核

- `13_周报汇总` 已按参考 workbook 的 `Monthly 前100趋势`、`Tier 年度分层` 和验收基准结构重新排布；计划部 BI、计划部 BSR Top100、market.db 父体快照三条基准紧邻展示，来源和“不可比明细参考”用途分开写明。
- `14_利润测试` 的字段顺序与 PD17 利润示例保持一致，公式链为 `净成交价→采购成本$→进口税$→仓储$→佣金$→单件利润$→利润率→SKU利润$`。单件利润在成本字段齐全时即可计算，SKU利润只有在补录 7 天销量后才计算，空输入不会被转成 0。
- `15_经营计划与实际` 直接复制计划部 `26年实际销量` 原表头和现有实际/目标值，新增 `1-6月实际合计、H1达成率、7月计划差额、执行状态` 四列；源表的 `#DIV/0!` 等错误单元格保留为空并在执行状态中提示补录。
- 独立回读确认：13 页范围 `A1:Q64`、14 页范围 `A1:AD25`、15 页范围 `A1:AK60`；13 页年度分层三行均有缓存值，14 页 K:R/Y/AD 公式均存在，15 页 AH:AK 派生公式均存在。

### 核心回勾

- 2025H1→2026H1 整体销量 `1,708,031→1,737,288`（`+1.71%`），销售额 `$102,753,078→$99,401,634`（`-3.26%`）。PP 销量 `992,489→984,309`（`-0.82%`），高客单价市场销量 `715,542→752,979`（`+5.23%`）。整体与 PP+高客单价市场的销量、销售额差额均为 0。
- GENIMO 以整体市场为主分母，整体销量份额 `12.07%→16.13%`；PP内份额另列为辅助。GENIMO不作为第三类，不能与 PP+非PP 再相加。
- 90 页保留缺失状态；当前代表输入有 37 条销量缺失、120 条销售额缺失，92 页标为“请复核”，不按 0 代替。
- 分类复核补充：同一父ASIN/ASIN Listing 组内只要任一子体标题含完整单词 `plastic`，该组即进入 PP；代表子体仍按 BSR、字段完整度和源行 ID选择。这样既保持父体合并，又避免因代表子体标题变化漏掉 PP 组，规则和原始/代表行可在 90/93 页回溯。

### 口径和边界

- 每月按父ASIN优先、无父ASIN时用ASIN形成Listing键；组内取最小可解析正整数BSR，BSR并列时取销量/销售额字段更完整行，再以源行ID决胜。原始行数和去重行数同时留存，源文件和数据库未被改写。
- 月度表、BSR Top100、粗细分层和 GENIMO 表的 MOM 统一为当前月 vs 去年同月；年度表单列 YOY（当前年 vs 上一年度）；2026.07 按实际源数据计算并标记覆盖状态。
- 参考 workbook 只用于核对结构、公式样例和领导参考值，不能覆盖当前源数据，也不能把参考表的正负号直接套到新结果。
- 本轮验证执行了 `node --check src/build_formula_market_xlsx.mjs`、生成脚本、XLSX 结构/公式/错误扫描和独立加总回勾。Excel 打开时按 `fullCalcOnLoad` 自动重算。
- 本轮额外用独立 SQLite 聚合脚本重算 H1 总量、PP/非PP互补分区、Top100逐月上限和 2026H1 粗分层；结果与工作簿缓存逐项一致，分区差额为 0，Top100 最大 Listing 数为 100。公式缓存类型与 15 页派生列也做了回读核验。
## 增量修复与口径复核 (2026-09-08) — Round 10

### 本轮修复

- BSR 解析器现在把数值、整数文本和 `N.0` 文本统一为正整数名次；多值仍取最小有效名次并保留审计标记。2026.01-07 若缺少竞品 raw 名次增强会直接失败，避免静默使用错误的 `.0` 解析结果。
- BSR Top100 明确采用“BSR≤100 后按 Listing 键稳定截取 100 条”的截断规则；并列名次保留并写入最大并列数，报告解释分层数量可能超过区间宽度。
- 2025.01-06 新增父体重复敏感性诊断（行数、父体键数、重复行、行汇总、首行和最大值代表结果）。在确认字段语义前，不自动把首行或最大值改写为历史主口径。
- 预测输出固定标注范围 `PP/plastic BSR Top100`，记录 `汇总` 工作表、源文件 SHA-256 和 `G3:J6`/`R2:T13`/`N3:P5` 单元格范围；参数区改为“静态假设，需外部重算”。GENIMO 建议不再用跨年方向值作为定量扩量基线。
- 报告生成时间默认取 `meta.imported_at`，主源 JSON 增加 SHA-256 身份；竞品替换完成后刷新 `meta.db_size_bytes`，并纳入回归检查。
- 复核发现竞品库重建器、竞品审计器和分析器对非零小数 BSR 的边界实现曾不完全一致；已统一为整数/`N.0` 才可解析、非零小数按缺失，并增加 `"591.0"`、换行多值、`"1.5"` 和 `0` 的回归检查。当前竞品审计为 51 项，失败 0；本次实际快照没有非零小数值，因此当前数据结果未发生变化。

### 当前验收

- `npm test`：128 项快速验收、54 张表/4,261,173 单元格全表审计、7 月竞品链路、分析和前端回归均需零失败；当前基线为 69,027 个信息性数值类型变化、竞品审计 51 项、分析审计 101 项、前端 43 张表/32,795 个单元格。
- 尚未自动解决的业务问题：2025 变体行中同一父体的销量/销售额字段语义仍未确认。本轮只增加可见的敏感性诊断和文档限制，未擅自选择首行、最大值或重写 2025 历史主口径；需要业务提供字段定义或父体汇总源后再决定是否调整。

## 严格可交付性整改与重新发布 (2026-08-31) — Round 9

### 本轮纠正

- 推翻 Round 7/8 中“2025 行级与 2026 父体级量级接近即可作为严格同比”的判断。两期时间集合一致，但统计单元不同；2026 年度行现为 `timeComparable=true`、`scopeComparable=false`，所有 2025→2026 百分比只标作方向性参考，不再据此断言精确市场增减。
- 2026 父体代表行从“源表首行”改为可审计规则：每个 Listing 键先取最小可解析小类 BSR；同名次优先月销量/月销售额字段完整行，再按源表顺序稳定决胜。7 份快照均由原始 Excel 重建，审计逐父体验证代表行是源表精确行且符合规则。
- 修复平均标价空值缺陷：仅统计非空、非空串、可解析的数值价格；49 个核心月从数据库反算，累计排除 175 个缺失/非数值价格行，月度和年度平均值均精确回勾。
- 2022-2025 源表没有 ASIN/父 ASIN，历史 BSR Top100 改为显式“源表行代理”。JSON/HTML 新增 43 个月质量诊断（候选行、截取行、标识覆盖率、不同名次数、重复名次行），跨年 BSR 变化只作方向性参考。
- 补齐 2026.09-12 四个月销量可能区间；所有历史月份展示统一为 `YYYY.MM`。HTML 新增整体销量、整体销售额、GENIMO 销量三张静态 SVG 趋势图，图表只使用 2026.01-06 同一父体口径。
- 新增由生成器维护的 `交付/户外地垫市场分析报告-极速版.md`；早期 HTML、根目录旧报告和旧速览稿移入 `docs/archive/`，当前交付目录只保留四个有效文件。

### 规则变更后的核心结果

- 整体市场：2026.01-06 销量 **1,737,288**、销售额 **$99,401,634**、加权成交均价 **$57.22**；相对 2025 同期的方向变化为 **-14.6% / -19.7%**。
- PP：销量 **984,309**、销售额 **$41,928,801**，占整体销量/销售额 **56.7% / 42.2%**；GENIMO 在 PP 中的销量/销售额份额为 **20.82% / 26.98%**。
- 高客单非 PP：销量 **752,979**、销售额 **$57,472,833**；GENIMO：销量 **280,198**、销售额 **$16,469,419**。
- 2026.06 整体销量 **558,956**、连续月环比 **+37.1%**，仍为核心期整体峰值。以上跨年百分比均为方向变化；2026 连续月环比属于同一父体统计口径。

### 验收

- 快速验收 128 项、主库 4,261,173 个单元格、竞品源链路 819,910 个单元格均需零失败。
- 分析审计 67 项通过；前端审计逐格核对 39 张表、24,770 个单元格，另校验 3 张 SVG 图、4 个预测区间、章节顺序、文本数字溯源与异常标记。
- Round 7/8 下列旧数字和“可直接参考”表述仅保留为变更历史，已被本轮规则和最终交付物取代，不得用于当前决策或对外交付。

## 领导反馈生产级审计与2026口径统一 (2026-08-31) — Round 8

### 审计结论

- 已按领导反馈将 **2026.01-07 整体市场趋势合并为一张表**。2026.01-06 逐月取核心全市场父体快照；2026.07 逐值取 94 父体源诊断数据，但 `coreComparable=false`，MOM/环比基准与结果均为 NULL，HTML 显示“不适用（口径不同）”，不参与累计，也不再保留独立 7 月附录趋势。
- 四个分类趋势分析、首页指标卡、总结卡和 GENIMO 量化建议全部改用 **2026.01-06 实绩**，2025 仅作为同周期比较基准。GENIMO Top 父体由原先混合全部年份累计改为仅累计 202601-202606，并按父 ASIN 优先的独立 Listing 键聚合。
- GENIMO 在 PP 中的 2026.01-06 销量/销售额份额按父体 family-aware 分类独立重算为 **20.86% / 27.03%**；分母为全部 PP 父体，分子为 GENIMO∩PP 父体，未用品牌总量替代 PP-only 分子。
- 清除了旧前端中的 2025 指标卡/洞察口径、旧“2026 每月 64-94 父商品”说明和独立“参考附录”导航，避免页面展示与当前数据库口径互相矛盾。

### 2026.01-06 核心复核结果

- 整体市场：销量 1,734,909、销售额 $98,910,171，较 2025 同期 -14.7% / -20.1%，加权成交均价 $57.01（同比 -6.4%）。
- PP：销量 982,203、销售额 $41,860,672，较 2025 同期 -14.3% / -9.7%，占整体销量/销售额 56.6% / 42.3%。
- 高客单非 PP：销量 752,706、销售额 $57,049,499，较 2025 同期 -15.2% / -26.4%，加权成交均价同比 -13.2%。
- GENIMO：销量 280,001、销售额 $16,332,081，较 2025 同期 +10.7% / +83.0%；2026.06 头部 8 个在榜 Listing 贡献品牌当月销量 83.5%，已作为集中度风险写入建议。
- 2026.06 为核心期整体销量峰值 559,122，连续月环比 +37.2%；补货、广告和新品测试建议以前置 4-8 周为执行窗口。2026.07 的 309,385 销量/$22,611,618 销售额只代表 94 父体样本，不与 1-6 月直接比较。

### 生产验收

- `verify.js`：128 项通过、0 失败。
- `full_audit.js`：54 张有效业务表、4,261,173 个单元格；结构问题 0、值差异 0；68,194 个数值存储类型变化仅作信息披露。
- `competitor_audit.js`：7 份快照、819,910 个单元格、43 项检查全部通过；每个 dedup 代表行均可回溯到原始精确行。
- `analysis_audit.js`：61 项全部通过，新增覆盖 2026.01-07 合并趋势、7 月跨口径禁算、2026 GENIMO PP-only 份额、2026 Top 父体周期及旧口径清理。
- `frontend_audit.js`：38 张 HTML 数据表、24,465 个单元格与 JSON/预测常量逐格一致；四类趋势逐字重算一致；章节顺序一至十一正确；21 项检查全部通过。
- `overwrite_guard.js`：覆盖关闭时拒绝重建，既有 DB 哈希保持不变。

### 仍需保留的口径边界

- 2025 为行级全市场导出（含变体行、无 ASIN 列），2026.01-06 为父体级全市场快照；规模量级相近且可用于同比参考，但颗粒度与导出日期差异仍须随表披露。
- 2025.05 小类 BSR 同值重复造成中部/尾部基准为空；相关 2026.05 MOM 和 2025.06 环比继续显示“无对应数据”，未插值、平移或编造。

## 2026.01-06 竞品快照更新重跑 (2026-08-28) — Round 7

### 变更背景

- 用户替换了 data/raw 下 2026.01-06 共 6 份竞品快照（批次 809631/809622/809620/809615/809606/809594 → 842085/842080/841734/841894/841906/841914）：新快照为**全市场父体级导出**（每父体一行代表子体，1135/1039/2002/1745/1691/2002 行，小类BSR 覆盖 1-40 万+），替代原"3000 行子体级展开、64-94 父体"口径；2026.07 未变（仍 3000 行/94 父体）。

### 处理与验证

- 新增 `src/build_competitor_db.js`（可复跑重建竞品库，填补仓库缺失的一环）：`raw_YYYYMM` 逐行镜像源表（尾部全空行保留为全 NULL 行，`raw:true` 取原始值、空串转 NULL、NUMERIC 亲和列保持数字类型）；`dedup_YYYYMM` 按 Listing 键取**首条记录**为确定性代表行（替代旧随机抽样后固化的做法），并记录源文件 sha256 与规则。
- `EXPECTED_ROWS`（apply）与 `EXPECTED_DEDUP`（竞品审计）更新为 1134/1038/1766/1744/1690/1993/94；竞品审计的 raw 行数检查改为按 Excel 实际行数，分组统计跳过无键的全空行。
- 分析层同步更新：2026 年度行 `scopeComparable` 由 false 改为 true（2026.01-06 全市场父体级 vs 2025 全市场行级，量级一致可直接参考），scopeNote 注明剩余颗粒度差异；验收基准 202602 复算为 **MOM 销量 -14.773%、MOM 销售额 -20.505%、环比销量 +9.553%、环比销售额 +23.801%**；报告口径说明、趋势分析口径提示、参考材料核对等处全部改为新口径文本。
- 2026.05 中部/尾部 MOM"无对应数据"（2025.05 BSR 重复，基准为空）的披露保持不变，2026.05 头部 MOM 基准仍为异常 100 行头部，仅作参考。
- **全部测试通过**：verify 全绿；full_audit 54 张表、4,261,173 单元格值差异 0；competitor_audit 43 项 0 失败；analysis_audit 50 项 0 失败（含 202602 新基准、2026 可比标记、全市场快照规模、无对应数据披露等新检查）；overwrite_guard 通过。

### 结论（新口径下）

- 2026.01-06 整体市场：销量 1,734,909（YoY -14.7%）、销售额 $98.9M（YoY -20.1%）、加权均价 $57.01（YoY -6.4%）；PP 销量 982,203（YoY -14.3%）但均价 +5.3% 逆势回升；高客单量额双降（-15.2%/-26.4%）；GENIMO 逆势量额双增（+10.7%/+83.0%）、均价 +65.2%。
- 跨年同比由"范围不可比"升级为"量级一致可直接参考"，2026 上半年市场整体回落、结构性分化（PP 价格企稳、GENIMO 升级）成为核心结论。

## 最新重扫与数据分析质量闭环 (2026-08-28) — Round 6

### 最终结论

- **数据链路通过**：`verify.js` 128 项全部通过；`full_audit.js` 按当前有效数据源核验 54 张业务表、3,694,439 个单元格，结构问题 0、值差异 0。
- **竞品源链路通过**：7 份竞品 Excel 共 1,365,000 个明细单元格与 `raw_YYYYMM` 值差异 0；每月父/ASIN键覆盖完整，64/74/82/79/88/91/94 条 `dedup_YYYYMM` 均能回溯到原始精确行。
- **分析公式通过**：`analysis_audit.js` 42 项全部通过；核心截止 202606、202607 仅附录；整体 = PP + high；Top100 每分类每月不超过 100；头中尾和五档均从同一 Top100 池切分并精确回勾；GENIMO 的 PP 销量/销售额份额均使用 PP-only 分子。
- **基准复现**：整体市场 202602 的 MOM 销量 -27.743%、MOM 销售额 -0.091%、连续环比销量 +9.309%、连续环比销售额 +33.525%（旧 64-94 父体口径；2026-08-28 快照更新后已复算为 -14.773%/-20.505%/+9.553%/+23.801%，见 Round 7）。
- **两层父体口径**：2026 销量/销售额/价格继续取固化代表行，确保验收基准不漂移；Top100/分层改为从同父体且符合分类的候选变体取最佳小类 BSR，禁止子体求和。
- **可复现性修复**：新增 `src/apply_competitor_2026.js` 和 `analysis_replacements`，标准 `npm run import` 现为基础 Excel 导入后确定性重放 7 个月竞品父体数据；测试不再把 2026 差异当作“预期失败”。
- **报告质量修复**：删除失效的 `cleanYoySales/cleanYoyRevenue` 两月列，改为 `scopeComparable/scopeNote`；修复四个分类错误复用整体市场验收提示；GENIMO 2027 的尺寸角色、利润/TACOS、晋级和 90 天退出门槛与参考 workbook 对齐。

### 已知限制

- 2026.01-06 为竞品父体快照（1038-1993 个父商品/月，Round 7 更新后为全市场父体级），2025 为全市场 SKU（约 1700-2000/月）；量级一致、跨年同比可直接参考，剩余差异为 2025 行级导出（含变体行、无 ASIN 列）vs 2026 父体去重的颗粒度差异，报告已随表注明。
- 主 Excel 的数值存储类型变化 68,226 处均已验证为数值等价，作为信息性统计保留，不属于值差异。

## 完整流程验收与优化路线 (2026-08-27) — Round 4

### 验收结论

- **数据转换**: 基本通过。重新从源 Excel 生成临时 SQLite 后，54 张有效业务表、4,441,989 个单元格结构问题 0、值不一致 0。
- **需求满足度**: 未完全通过，约 70%。主要缺口不在原始值导入，而在分析口径、最新月份覆盖、最终 HTML 完整度和流程可复现性。
- **工作表范围**: 50 张有效月度表 + 4 张 TOP 表；其中 31 张历史月份在 Excel 中隐藏，但原始 TXT 要求“历年”及年度/月度 YoY、MoM，应当保留。

### P0 — 必须先修复

#### P0.1 “小类前 100”口径错误 [已在优化版修复]

- **当前实现**: 每月按 `月销量` 重新排序后取 Top 100。
- **原始要求及源字段**: 应优先依据 `小类BSR` 的 1-100 范围分析，并按 1-5、6-10、11-20、21-50、51-100 分层。
- **影响**: 整体、PP、高客单三套 Top100 及分层趋势均可能改变。
- **改造**: 建立可审计的 `小类BSR` 解析规则；多值/换行 BSR 单独标记；输出按 BSR 与按销量排名两个视角，主报告以 BSR 口径为准。

#### P0.2 2023 年度 YoY 使用不可比周期 [已在优化版修复]

- **当前实现**: 2023 完整 12 个月对比 2022 年 6-12 月共 7 个月。
- **影响**: 报告中的 2023 整体 +246.3%、PP +209.0%、高客单 +273.0%、GENIMO +823.3% 不是同周期同比。
- **改造**: 年度 YoY 仅比较完整自然年；2023 改为 2023.06-12 对比 2022.06-12 的同期同比，并明确标注 YTD/同周期。

#### P0.3 2026.03-2026.07 数据口径异常未闭环

- **现状**: 报告直接排除 5 张可见月份表；2026.04 起销量从 29.1 万跳到 292.5 万并维持百万级。
- **核验**: 异常来自源 Excel，不是导入错位。
- **改造**: 生成原始口径、可比口径和异常诊断表；在无法确认卖家精灵口径前，不将异常月份用于长期结论，但必须披露并附原值。

#### P0.4 分析过程不可复现 [已在优化版修复]

- **现状**: 缺少指标计算、报告生成和可复跑的全量审计脚本。
- **改造**: 增加统一分析程序，从 SQLite 生成结构化中间结果、Markdown 和独立 HTML；所有报告数字由代码生成，禁止手工硬编码核心指标。

### P1 — 高优先级修复

#### P1.1 高客单筛选口径不一致 [已在优化版统一NULL语义]

- **报告文字**: `标题 NOT LIKE plastic AND (标题 LIKE polypropylene OR 价格 >= 40)`。
- **实际差异**: 空标题且价格 >= 40 的 51 条记录被统计逻辑纳入，但展示 SQL 会因 NULL 三值逻辑排除；报告写 29,391 条，按展示 SQL 回算为 29,340 条。
- **改造**: 使用 `COALESCE(商品标题, '')` 固化 NULL 语义；将“材质命中”和“仅价格命中”拆分统计；补充 sandwich、woven、polypropylene 等可配置关键词表。

#### P1.2 均价定义不透明 [已在优化版提供双口径]

- **现状**: 月度均价为 SKU 标价简单平均；年度均价近似月均价再平均，不是 `销售额/销量`。
- **改造**: 同时输出 `SKU平均标价` 与 `销量加权成交均价=销售额/销量`；报告标题和公式必须明确。

#### P1.3 最终 HTML 信息不完整 [已在优化版修复]

- **现状**: Markdown 有完整月表，HTML 只展示代表月份和概括结论。
- **改造**: HTML 内提供完整月度 MoM/YoY、年度同周期同比、Top100分层表和口径说明；摘要与明细可折叠，但不能缺失。

### P2 — 工程质量修复

- 修复 `IMPORT_OVERWRITE=false` 仍覆盖数据的问题。
- 空表依据 `!ref` 判断，不再写死 `Sheet6`。
- 严格识别月份表和 TOP 映射；未知工作表必须明确报错或记录跳过原因。
- 恢复 `tests/full_audit.js`，区分值不一致和存储类型变化。
- 落实或移除无效的 `IMPORT_NUMERIC_TOLERANCE`。
- 在 meta 或 `sheet_catalog` 中记录工作表顺序、可见性、有效区域、分类、目标表和行数。
- 修正 WAL 模式下 `meta.db_size_bytes` 的统计时点。
- 为跨月查询建立统一视图/规范化分析层，处理日期、混合类型、缺列和数值存为 TEXT 的情况。

### 完成标准

1. 重新导入后 54 张业务表全量逐 cell 值不一致为 0。
2. `IMPORT_OVERWRITE=false` 保护测试通过，原 DB 哈希不变。
3. 所有核心指标均由单一分析脚本生成，报告中不存在手工维护的核心数字。
4. 整体、PP、高客单均覆盖月度 MoM、月度 YoY、年度同周期 YoY、小类BSR前100和五档分层。
5. GENIMO 建议可追溯到品牌份额、价格带、尺寸和产品表现数据。
6. 2026.03-07 原值完整披露，异常口径有明确处理状态。
7. Markdown 与 HTML 数字一致，HTML 可独立打开且包含完整明细。

### Round 4 实施结果

- 转换 schema 升级至 1.1.0，新增 `sheet_catalog`，完整记录 55 张源工作表的可见性和处理结果。
- `IMPORT_OVERWRITE=false` 改为目标存在时立即拒绝；保护测试确认 DB 哈希不变。
- 恢复 `tests/full_audit.js`：54 张表、4,441,989 cells、结构问题 0、值不一致 0、数值存储类型变化 70,989。
- 修复 `meta.db_size_bytes`：优化临时库 meta 与最终文件大小均为 52,764,672 bytes。
- 新增 `src/analyze_market.js`，一次运行生成结构化 JSON、完整 Markdown 和独立 HTML。
- 优化版以 `小类BSR` 解析名次做前100及五档分层；2023 YoY 改为 2023.06-12 对比 2022.06-12。
- 优化版同时展示 SKU 平均标价和销量加权均价，完整列出整体、PP、高客单、GENIMO的月度/年度/分层指标。
- 2026.03-07仍为待业务确认项：原值完整进入异常附录，暂不进入长期可比结论。

## 复核发现 (2026-08-27) — Round 3: 工作表范围与转换可复验性

### 口径 #1: 55 张不是 Excel 界面可见子表数 [已更正文档]

- **工作簿内部**: 55 张工作表。
- **Excel 界面可见**: 23 张（4 TOP + 2025.01 至 2026.07 共 19 张月度表）。
- **隐藏**: 32 张，其中 31 张是 2022.06 至 2024.12 的有效历史月度表，1 张 `Sheet6` 无有效区域。
- **有效导入**: 54 张业务表（50 月度 + 4 TOP）。
- **分析决策**: 原始 TXT 要求分析“历年”并计算年度/月度 YoY、MoM，故保留 31 张隐藏历史表；隐藏状态不等于无效数据。

### Bug #5: `IMPORT_OVERWRITE=false` 不能防止覆盖 [待修复]

- **现象**: 配置为 false 时只是不删除 DB 文件；随后仍会删除并重建 `meta` 和每张同名业务表。
- **影响**: 配置名和 SPEC 暗示的保护语义失效，存在误覆盖已有 DB 的风险。
- **建议**: 当目标 DB 已存在且 `IMPORT_OVERWRITE=false` 时立即报错并非零退出。

### Bug #6: 空表识别依赖固定名称 [待修复]

- **现象**: `classifySheet(name)` 只把名称为 `Sheet6` 的表判为空，未依据 `sheet['!ref']`。
- **影响**: 源文件新增其他空白表时会进入月度处理并可能异常；名为 `Sheet6` 的有效表也会被误跳过。
- **建议**: 先按 `!ref` 判断无有效区域，再按严格月份格式或 TOP 映射分类，未识别表应报错或明确跳过。

### 审计缺口 #1: 全量审计脚本缺失 [待修复]

- **现象**: README、TASKS、REVIEWS 均声称 `full_audit.js` 校验 4,441,989 cells，但当前仓库没有该文件。
- **影响**: “零数据丢失”无法从当前交付物复验。
- **当前结论**: 现有 `tests/verify.js` 的 61 项检查通过，只能证明行数、meta 和少量抽样一致，不能替代全量逐 cell 审计。

### 独立复核 #1: 当前 Excel 与 DB 的全量值对比 [通过但未脚本化]

- **范围**: 54 张有效业务表，4,441,989 个源数据单元格。
- **结构问题**: 0。
- **值不一致**: 0（数值按数值等值比较，文本按字符串精确比较，空单元格对应 NULL）。
- **类型变化**: 70,989 个 Excel 数值在 SQLite 中以 TEXT 存储，主要由动态整列类型推断在混合类型列中选择 TEXT 导致。
- **影响**: 数据值未丢失，但依赖 SQLite 原生类型的排序、比较或外部 BI 类型识别可能出现差异；查询时应显式 `CAST`，或后续增加源单元格类型元数据/统一视图。

### Bug #7: `IMPORT_NUMERIC_TOLERANCE` 配置未实现 [已移除无效配置]

- **现象**: `.env.example` 和 SPEC 声明该配置，主转换脚本没有读取。
- **影响**: 用户修改该变量不会产生任何效果。

### Bug #8: `meta.db_size_bytes` 不是最终文件大小 [待修复]

- **现象**: meta 记录 51,113,984 bytes，当前 `market.db` 实际为 52,760,576 bytes。
- **根因**: WAL 模式下在 checkpoint 和关闭连接前读取主 DB 大小。
- **影响**: 不影响业务数据，但 meta 的文件大小字段不准确。

### 数据质量 #10: 2026.04 - 2026.07 市场指标数量级突变 [待业务确认]

- **月销量汇总**: 2026.03 为 291,479；2026.04 为 2,924,738；2026.05 为 5,387,705；2026.06 为 7,583,673；2026.07 为 5,302,834。
- **核验**: 全量逐 cell 对比为 0 value mismatch，确认异常来自源 Excel，不是数据库列错位或转换错误。
- **影响**: 直接纳入 MoM、YoY、年度累计会主导全部趋势，必须先确认统计周期、抓取范围或卖家精灵口径是否改变。

## 自审发现 (2026-08-27) — Round 1: 严重 bug

### Bug #1: TOP 表 header 行错位 [已修复]

- **现象**: 4 张 TOP 表的列名错位为数字 (e.g. `_1, _436710, _13063`)
- **根因**: `processTopSheet` 硬编码 `headerRow = 1`, 但 4 张 TOP 表的 header 实际在 row 0
- **影响**: 4 张 TOP 表共丢失 1 行 / 表, 且列名错误
- **修复**: 将 `headerRow = 1` 改为 `headerRow = 0`
- **验证**: `full_audit.js` 全表 4,441,989 cells 0 mismatch

### Bug #2: TOP销量 (倍率) 空 header 列含数据被丢弃 [已修复]

- **现象**: TOP销量 (倍率) 的 col 18, 20, 22, 24 位置 header 为空, 但数据行在这些列有值
- **根因**: 脚本在 CREATE TABLE 和 INSERT 时都 `if (!headers[i]) continue;` 直接跳过空 header
- **影响**: TOP销量 (倍率) 共 3697 cell 丢失
- **修复**: 空 header 列改为占位名 `_col_<index>`, 保留数据
- **验证**: `full_audit.js` 全表 0 mismatch, TOP销量(倍率) 3697 cell 找回

### Bug #3: tests/verify.js TOP 表 header 检测错误 [已修复]

- **现象**: verify.js 中 `detectHeaderRow` 对 TOP 表返回 1
- **修复**: 改为返回 0 (与 import_xlsx.js 一致)
- **验证**: `verify.js` 全部 PASS, 行数匹配

### Bug #4: tests/verify.js 抽样对比取样范围不一致 [已修复]

- **现象**: Excel 取 "前3+后3", DB 取 "前6", 不一致导致 mismatch
- **修复**: `getDbSample` 改为先取前 3 + 后 3, 与 Excel 对齐
- **验证**: 抽样对比全部 PASS

## 自审发现 (2026-08-27) — Round 2: 源数据质量问题

> 经 `full_audit.js` 全表逐 cell 校验 (4,441,989 cells) 确认 0 mismatch.  
> > 下面记录的是**源数据本身**的不一致性, 属于数据质量问题, 不影响 lossless 保证.

### 数据质量 #1: `上架时间` 列类型不一致 [已知]

- **现象**: 同名列在不同 sheet 类型不同
  - 旧 sheet (202206-202212): Excel `cell.t=s` (字符串 "2020-11-18") -> DB TEXT
  - 新 sheet (202406+): Excel `cell.t=n` (序列号 44105 = 2020-10-19) -> DB INTEGER
- **原因**: Excel 文件早期手动输入 (文本), 后期改为 Excel 日期格式 (序列号)
- **影响**: 跨 sheet 查询需要类型转换 (例如 `WHERE 上架时间 > "2024-01-01" OR CAST(上架时间 AS TEXT) > "2024-01-01"`)
- **当前决策**: 不修改, 保留源数据原貌 (lossless 原则)
- **后续建议**: 可在 SPEC.md 增加可选的 `--normalize-dates` 后处理, 把 Excel 序列号 (30000-50000) 转为 ISO 字符串

### 数据质量 #2: `子体销量` / `子体销售额` 列类型不一致 [已知]

- **现象**: 旧 sheet: TEXT (空值多, 无 INTEGER 样本) -> DB TEXT  
  新 sheet: INTEGER (有真实数据)
- **原因**: 旧 sheet 数据未填充, 类型推断退回 TEXT
- **影响**: 同 #1, 跨 sheet 查询需要类型转换
- **当前决策**: 不修改

### 数据质量 #3: `Coupon` 列类型不一致 [已知]

- **现象**: 旧 sheet: TEXT (空值多) -> DB TEXT  
  新 sheet: REAL (0.4 = 40% 折扣, 22.67 = $22.67 off)
- **原因**: 旧 sheet 无数据, 新 sheet 有不同格式 (百分比 vs 金额)
- **影响**: 同 #1
- **当前决策**: 不修改 (两种格式语义不同, 应由消费者判断)

### 数据质量 #4: `LQS` 列 5 张表为 REAL [已知]

- **现象**: 45 张 INTEGER, 5 张 (2026.3-2026.7) REAL (6.1, 6.4 等小数)
- **原因**: 源数据精度差异
- **当前决策**: 不修改

### 数据质量 #5: `月销量增长率` 在 4 张新 sheet 缺失 [已知]

- **现象**: monthly_202604 / 202605 / 202606 / 202607 缺少 `月销量增长率` 列
- **原因**: 源 Excel 没有该列
- **影响**: 跨 sheet 查询该列会报错 (UNION ALL)
- **当前决策**: 不修改, 反映源数据真实结构

### 数据质量 #6: `买家运费` 仅在 26 张 sheet 存在 [已知]

- **现象**: monthly_202604 起新增 `买家运费` 列, 之前 24 张没有
- **原因**: 源 Excel 新版本加列
- **当前决策**: 不修改

### 数据质量 #7: 小类 BSR 含换行符 [已知]

- **现象**: 旧 sheet 部分行的 `小类BSR` 值为 "94\r\n260" (多 BSR 排名)
- **原因**: 源 Excel 单元格内有换行
- **当前决策**: 保留 TEXT, 不拆分

### 数据质量 #8: `卖家信息` 含换行符 [已知]

- **现象**: 326/494 行 in monthly_202206 有多行内容 (公司名 + 地址)
- **原因**: 源 Excel 单元格内有换行
- **当前决策**: 保留 TEXT

### 数据质量 #9: 重复列名 [已知]

- **现象**: 旧 sheet (202206-202406) 的列 `重量` 和 `体积` 各出现两次 (磅 + 千克)
- **处理**: `_2` 后缀区分 (e.g. `重量`, `重量_2`)
- **改进**: 可后续按值特征智能判断是磅还是千克, 但当前实现符合 lossless 原则

## 结论

- **Round 1**: 修复 4 个真实 bug (列错位 / 空 header / 验证脚本逻辑)
- **Round 2**: 0 个真实 bug, 9 个源数据质量问题已记录
- **Lossless 保证**: `full_audit.js` 全表逐 cell 比对 4,441,989 cells, 0 mismatch
- **SPEC.md** 已更新: 第 4 节增加 7. 空 header 列保留策略
- **TASKS.md** 已更新: Phase R 增加 5 个修复任务 (R1.1 ~ R1.5)


## 数据口径修正与最终交付 (2026-08-27) — Round 5

### 结论
- 2026.03-07 源 Excel 为子体级展开口径（每月 3000 子体行，销量虚增约 10 倍），当时用竞品快照按父 ASIN 去重替换；2026.01-02 同步统一。代表行现已在 Round 6 固化为可审计快照并支持确定性重放，不再重新随机选择。
- 2026 全年统一竞品父 ASIN 去重口径（每月 64-94 个父商品），月度销量曲线恢复平滑（8.9万→39.6万→27.0万），无暴增。
- 分析口径采用用户 B 方案：MOM=跨年同月、环比=连续月、年度 YoY=同周期；Round 6 已取消容易误解的 2026.01-02 “可比口径列”，改为 2026.01-06 同月份数学同比并显式标注范围不一致。
- 要求内容.txt 全项覆盖：整体/PP/高客单/GENIMO × 月度/年度/BSR前100/头中尾/五档/趋势分析。
- 全量核验：PP/高客单/GENIMO 筛选与 db 独立重算完全一致；2025/2026 年度总量一致；MOM 口径数值验证一致。
- 部署：gh-pages 分支上线（Zhouchengye-0914.github.io/t5-outdoor-rug-market-analysis）。

### 遗留限制
- 2026 竞品口径 vs 2025 全市场口径的跨年同比存在范围差异（报告已标注，提供可比口径列）。
- 2026 年商品数 64-94 < 100，BSR 51-100 档稀疏。
- 需要 2025 年竞品快照才能完全同口径跨年对比。

## SPEC 2.0 复核 (2026-09-11): 月度子表表头行错位

### Bug #10: 2025.10—2026.07 被误判为无数据 [已修复]

- **现象**: 原始工作簿的 `2025.10`—`2026.07` 子表均包含明细，但读取器固定把第 2 行当作表头，导致这些表的第 1 行真实表头被当作数据表头，字段索引全部失配，结果 XLSX 显示为“无数据”。
- **根因**: 卖家精灵导出结构发生变化：旧月份为“第 1 行导出说明 + 第 2 行表头”，后续月份为“第 1 行表头”；读取逻辑未识别两种结构。
- **修复**: `src/build_spec2_html.mjs` 新增前 9 行表头探测，使用 ASIN 与品牌/商品标题稳定字段定位真实表头；数据行从探测到的表头下一行开始读取，并记录 `headerRow`。
- **独立核验**: 修复后主源读取 73,810 行、去重 63,554 个 Listing；2025.10—2026.07 各月均 `有数据`，对应原始行数为 1,749、1,743、1,827、1,239、1,045、2,000、2,000、2,000、2,000、2,000；最新月 2026.07 结果页销量 334,087、销售额 $20,742,132，整体=PP+高客单价市场的销量/销售额差额均为 0。
- **交付同步**: HTML 与公式 XLSX 已重新生成；XLSX 的 `90_原始输入`、`91_去重明细`、`92_聚合输入` 和 `93_数据校验` 保留表头行、状态及回勾链，避免再次把解析失败解释成源数据为空。

## SPEC 2.0 口径与交付复核 (2026-09-11)

- 月度业务表头统一为 `月份｜整体/范围Listing｜整体/范围销量｜整体/范围销售额($)｜平均标价($)｜加权成交均价($)｜销量MOM｜销售额MOM｜Top100 Listing`（XLSX 同时保留有效数和 Top100 销量/销售额列）。这里的 MOM 明确定义为“本月 vs 去年同月”；年度表单列 `销量YOY`、`销售额YOY`，按当前年与上一年度的共同覆盖月份计算，并显示年度覆盖状态，不再把连续自然月变化作为另一组业务列。
- 第三部分的交付名称统一为“高客单价市场”。其程序内部范围键仍为 `nonpp` 以保持代码兼容，但业务定义是整体市场排除 PP 后的完整补集，不增加价格门槛；整体=PP+高客单价市场的销量、销售额差额为 0。
- HTML 导航补充 `第一部分｜整体市场`、`1.1`—`1.7`，第二、三部分依次使用 `2.1`—`2.7`、`3.1`—`3.7`，GENIMO 使用 `4.1`—`4.6`；原始毛利率、FBA、Coupon 仅在 XLSX 原始证据页保留，不进入前端分析表，也不参与利润推导。
- 新工作簿沿用已验证的新文件结构：`01_整体市场月度`、`02_PP市场月度`、`03_高客单价市场月度`、`04_GENIMO品牌月度`、`05_年度YOY`、`06_BSR分层` 加 90—94 证据/校验页；所有展示公式写入 XLSX，可通过 `src/dump_spec2_dataset.mjs` 与 `src/build_spec2_xlsx.py` 复现。
- 并列 BSR 的处理已统一为“每月、每个范围最多100个 Listing”：按 BSR 升序后以 Listing 键和源行号稳定截取；正式文件 2026.07 整体 Top100 为 100 条，PP 为 67 条，高客单价为 44 条，HTML 和 XLSX 的分层均从各自范围的同一 Top100 池计算。
