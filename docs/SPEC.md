# SPEC: T5 户外地垫市场数据 -> SQLite 转换工程

## 0. 规范优先级与数据变更登记

- **业务定义不得漂移**：`要求内容.txt` 与 `新增参考的材料和内容/新增说明情况.txt` 中已经确认的分析范围、MOM/环比/YoY 定义、BSR Top100 范围、截止月份和 2027 决策要求是验收上位规范，后续数据替换不得静默改写这些业务规则。
- **用户确认的数据替换决定当前有效数据源**：数据替换不改变业务公式，但会改变由数据计算出的数值基准。SPEC 必须同时保留旧口径历史值和新口径当前值，登记发生日期、文件/批次、替换前后行数、影响月份、旧新基准差异及可比性影响；不得把旧值硬编码到新数据，也不得因保留原始文字而擅自回退用户已经替换的数据。
- **证据优先级**：当前用户明确说明 > 当前 `data/raw/` 中用户确认的有效数据 > 两份需求 `.txt` 的业务规则 > 参考 workbook 的结构/公式样例 > 历史提交。发生术语或数值冲突时必须显式记录映射或版本差异，不能择一静默覆盖。

### 0.3 最新月度比较口径覆盖（2026-09-04）

- 用户本轮明确澄清：交付物中的“月度环比/MOM”统一指**当前月份与去年同月的比较**，例如 `2026.02 vs 2025.02`、`2026.03 vs 2025.03`；该指标使用 `momBasis` 和 `mom*` 字段。
- **不再计算或展示“本月 vs 上月”的连续环比**。因此 `2026.03 vs 2026.02` 的 `+120.5%` 销量和 `+94.7%` 销售额不属于本次交付指标，必须从 JSON、Markdown、HTML、摘要和结论中移除；不得以 `chain`、`previous` 或其他别名继续输出。
- 年度 `YoY` 仍按同月份集合累计比较，例如核心 `2026.01-06 vs 2025.01-06`，不改为月度百分比的平均值。
- 本节是对此前 SPEC 中“同时展示本月 vs 上月环比”的最新用户口径覆盖。`新增说明情况.txt` 第③条保留为历史要求证据，但执行时以本节最新直接指令为准；后续如恢复上月比较，必须由用户再次明确确认并先更新 SPEC。

### 0.1 已登记的数据替换

- **登记时间**：2026-08-28；**来源**：用户提供并放入 `data/raw/` 的 `Competitor-US-2026.01-842085.xlsx`、`842080`、`841734`、`841894`、`841906`、`841914`（2026.01-06）及 `809440`（2026.07）。
- **替换范围**：`market.db` 的 `monthly_202601` ~ `monthly_202607`；替换前为约 64-94 个父体/月，替换后为 2026.01-06 全市场父体级快照（1134、1038、1766、1744、1690、1993）及 2026.07 的 94 父体快照。原始子体镜像和确定性去重记录保存在 `competitor_809440.db`。
- **影响**：替换后的 2026 数据与旧 64-94 父体口径不是同一数据集。旧口径 202602 的 `-27.7% / -0.1% / +9.3% / +33.5%` 保留为《新增说明情况》给出的历史校验值；当前交付仅按跨年同月 MOM/环比重新计算为销量 `-14.8%`、销售额 `-20.5%`。此前由“本月 vs 上月”得到的 `+9.6% / +23.8%` 仅是旧连续环比派生值，不属于当前交付指标，不得继续写入输出。
- **当前状态**：当前工作树和当前交付必须使用用户已替换的 2026.01-06 全市场父体级数据；当前数值验收以替换口径为准，不要求恢复旧 64-94 快照。报告必须说明旧值为何不再复现，并证明新值由当前数据和同一公式计算得到。

### 0.2 计划部核对 workbook 登记（2026-09-03）

- **文件**：`新增参考的材料和内容/销量预测计划部底表-户外地垫.xlsx`；工作簿共 15 张表，10 张可见、5 张隐藏，最后修改时间为 2026-09-03 17:48:58（本地文件时间）。该文件是新增参考材料，当前实际路径以此处为准；不再假定 `data/raw/` 下存在同名副本。
- **用途边界**：该文件是计划部/领导提供的销量预测与口径核对底表，不是当前 `market.db` 的自动导入源。其 `行业大盘数据` 使用 `BI看板-亚马逊后台` 的 `Outdoor Rugs` 全类目数据；2.8% 不能直接解释为 `plastic`/PP 子集增速，也不能改写 `market.db` 明细。按 0.4.1 的最新领导验收决策，它作为报告的“领导验收主基准”单独呈现。
- **已确认的数学基准**：`行业大盘数据!H4:H9` 合计 2025H1 `1,781,765`，`I4:I9` 合计 2026H1 `1,831,843`，`1,831,843 / 1,781,765 - 1 = +2.8105838873%`。`行业大盘数据!D6` 与 `基础销量预测-计划端!C3` 当前均为该数值的硬编码副本；正式复核必须保留独立求和公式和来源字段。
- **与当前报告的关系**：当前报告 `market.db` 的整体市场 2025H1 `2,033,235`、2026H1 `1,737,288`，计算为 `-14.5554744041%`；两组数字来自不同源文件、覆盖/去重范围和统计单元，不能把负号直接改成正号。最新决策不是把计划表导入或覆盖市场库，而是将计划部 BI 作为领导验收主基准、将 market.db 负值保留为不可比明细参考。
- **公式状态**：计划 workbook 由 Go Excelize 生成，`calcMode=auto` 但本次新增文件的公式缓存值大量为空（`data_only` 读取为 `None`；旧副本也可能显示 `0`）；缓存空值/零值不作为业务结果。任何交付前核对须采用 Excel/LibreOffice 重算或独立脚本复算，并记录公式、分子、分母、月份范围和结果。

### 0.2.1 参考文件路径修订（2026-09-04）

- 用户新增确认的参考文件实际位于 `新增参考的材料和内容/销量预测计划部底表-户外地垫.xlsx`，本轮核对必须直接读取该文件；路径修订不改变其业务口径，也不把它变成 `market.db` 的自动导入源。
- 本轮核对范围：原表公式/输入的可重算性、`行业大盘数据` 的 2025/2026 H1 汇总及 `2.8%`、`BSR底表-美国` 的 Q=1..100 H1 汇总/增速、以及与当前报告数据源的差异。先完成公式重算和证据登记，再决定是否需要更新报告或 SPEC 后续章节。
- **本轮独立重算结果（2026-09-04）**：`行业大盘数据!H4:H9` 合计 `1,781,765`，`I4:I9` 合计 `1,831,843`；按 `SUM(I4:I9)/SUM(H4:H9)-1` 重算为 `+2.8105838873%`，与 `D6`、`基础销量预测-计划端!C3` 的存储值一致。该值是 BI 看板的全类目 `Outdoor Rugs` H1 同比，不是 PP 子集，也不是当前 `market.db` 口径。
- **BSR 独立重算结果（2026-09-04）**：按 `Q=1..100` 直接对月度输入列 `AX:BC`（2025H1）和 `BJ:BO`（2026H1）求和，得到 `1,066,818`、`1,121,130`，增幅 `+5.0910277104%`。原表 `P` 列公式为 `=SUM(BJ:BN)+BP`，漏掉 6 月 `BO`、混入 7 月 `BP`；若机械执行该错误公式，结果为 `1,018,734`、增幅 `-4.50723553596%`，不得采信。另有 `M:P` 汇总公式只填到 Q=78，Q=79..100 的月度输入仍有数值但无 H1 汇总公式；`F:H` 公式引用的 B:E 为空，且 L/R 分组存在空白，故该表本身未达到可直接交付的公式完整性。
- **口径边界**：上述 `+5.0910277104%` 是计划部 workbook 自身 BSR Top100 100 行、2025H1 对 2026H1 的重算结果；不替代当前报告的市场 DB 或当前报告 BSR Top100。跨源差异必须并列披露，不得为了符号一致而改写任一来源。

### 0.4 最新领导验收基准待对账（2026-09-04）

- 用户最新确认：按领导验收要求，当前要交付的“2026 相比 2025”结论应体现为**上升**；该方向在新增业务表中已有体现。此条是当前问题的验收目标，优先级高于此前“仅按市场 DB 负向结果交付”的暂定判断。
- 本条不授权直接把 `-14.5555%` 改写成正数。修正前必须追溯新增业务表的统计单元、月份映射、类别筛选、去重规则和汇总字段，并与原始明细及 `market.db` 逐层对账，确定是数据处理明细错误还是权威源/范围不同。
- 必须同时保留两条可复核轨迹：①领导新增业务表中产生正向结果的原始字段、公式、分子、分母和月份范围；②当前报告实际采用的数据源、过滤、去重、汇总和结果。若两者范围不同，交付中必须明确“领导验收口径”与“市场 DB 口径”，不得混列为同一指标。
- 本轮后续动作顺序固定为：先锁定本条验收基准和原始业务表字段 → 再更新本 SPEC 的权威源/公式映射 → 再修正导入或分析代码 → 重新生成 JSON/Markdown/HTML → 重新执行原表、公式、结果和线上发布审计。未完成对账前，不得发布新的方向性结论。

#### 0.4.1 本轮对账决策与交付实现（2026-09-04）

- 对账结论：领导新增业务表的 `行业大盘数据` 与 `BSR底表-美国` 均显示 2026 H1 销量高于 2025 H1。前者 `1,831,843 / 1,781,765 - 1 = +2.8105838873%`，后者按 `Q=1..100` 的原始月度列 `BJ:BO` 对 `AX:BC` 独立求和为 `1,121,130 / 1,066,818 - 1 = +5.0910277104%`。
- 权威呈现：整体市场“领导验收主基准”采用该计划部 BI 全类目销量（仅销量），BSR Top100 正向值作为同 workbook 的辅助验收基准；两者均保留来源工作表、分子、分母、公式/列范围。参考 workbook 不提供销售额或均价，交付中不得补推这些字段。
- 市场库边界：`market.db` 的 2025 H1 行级含变体与 2026 H1 父体去重快照不是同一统计单元，当前 -14.5554744041% 销量 / 约 -19.7% 销售额仍保留在 JSON、明细表和结论中，但统一标注为“不可比明细参考”，不得作为领导验收主结论，也不得直接改写成正数。
- 输出要求：正向验收基准必须出现在 JSON 顶层 `leadershipBenchmark`、Markdown 整体市场章节/趋势结论、HTML 独立“领导验收主基准”区块及极速版；负向 market.db 结果必须与其并列且清楚标注来源和统计单元差异。
- 回归要求：除原有原表/DB/前端逐格审计外，必须校验 `leadershipBenchmark.industry` 的 2025/2026 H1 分子分母和 `+2.8106%`，以及 `leadershipBenchmark.bsrTop100` 的 `+5.0910%`；禁止重新引入本月 vs 上月连续环比字段或把计划表预测/目标当作历史实绩。

### 0.5 原始 2026 数据回切可比性核验（2026-09-04）

- 用户新增核验假设：当前任务可能应使用 `data/raw/地垫-卖家精灵市场数据.xlsx` 中未替换的 2026 原始月度数据，并要求最终计算结果与计划部新增 workbook 的正向结果一致；在证据确认前，不得回切 `market.db`、删除 `analysis_replacements` 或改写现有交付结论。
- 本轮必须分别重算三组数据：①主源 workbook 的 2025/2026 原始月度销量；②计划部 `行业大盘数据!H4:I9` 的 BI 全类目销量；③当前 `market.db` 用户确认替换后的父体快照。统一记录月份、分子、分母、公式、统计单元和差异，不得只比较最终百分比。
- 回切判定：只有在原始 2026 数据与计划部最终表满足相同月份范围、相同商品范围/统计单元、相同字段定义，且逐月及 H1 合计可复核一致时，才允许把原始 2026 数据恢复为当前主分析源；若仍不一致，必须保留现有替换数据和双轨呈现，并说明计划部结果不能由主源原始明细直接复现。
- 本轮动作边界：仅做只读对账和独立公式重算；不新增业务数据、不修改原始 workbook、不删除用户替换记录。完成对账后，若需要改变主源或输出权威口径，必须再次更新本 SPEC 并重新生成、审计、发布。

#### 0.5.1 本轮原始数据与计划表对账结果（2026-09-04）

- 主源 workbook 逐月直接求和（表头识别为 `月销量` / `月销售额($)`，不使用源表增长率列）得到：2025 H1 销量 `2,033,235`、销售额 `$123,849,866`；未替换的 2026 H1 原始销量 `16,421,197`、销售额 `$1,088,323,028`，按 `SUM(2026.01-06)/SUM(2025.01-06)-1` 分别为 `+707.6389%`、`+778.7438%`。这不是计划部最终表中的 `+2.8%`。
- 计划部 `行业大盘数据!H4:I9` 独立重算为 `1,781,765 -> 1,831,843`（`+2.8106%`）；计划部 `BSR底表-美国` 的 `Q=1..100` 原始月度列独立重算为 `1,066,818 -> 1,121,130`（`+5.0910%`）。计划表的 2025 基线本身也不等于主源 workbook 的 `2,033,235`，因此不是同一汇总结果。
- 2026 月度销量对账（单位：件）：

  | 月份 | 计划 BI 2025 | 计划 BI 2026 | 主源原始 2025 | 主源原始 2026 | 原始 2026 / 计划 2026 - 1 | 当前 market.db 2026 |
  | --- | ---: | ---: | ---: | ---: | ---: | ---: |
  | 2026.01 | 101,991 | 87,343 | 111,578 | 116,036 | +32.9% | 104,553 |
  | 2026.02 | 123,187 | 109,170 | 134,396 | 117,566 | +7.7% | 114,541 |
  | 2026.03 | 255,621 | 275,204 | 247,902 | 291,479 | +5.9% | 252,620 |
  | 2026.04 | 364,728 | 369,265 | 382,988 | 2,924,738 | +692.0% | 299,007 |
  | 2026.05 | 484,252 | 471,247 | 617,816 | 5,387,705 | +1,043.3% | 407,611 |
  | 2026.06 | 451,986 | 519,614 | 538,555 | 7,583,673 | +1,359.5% | 558,956 |

- **主源 ASIN 读取口径已纠正（2026-09-04）**：主源 workbook 的 50 张月度明细表的 `ASIN` / `父ASIN` 并非空值；它们以 Excel 富文本+超链接保存，SheetJS 的 `cell.v` 为空，但 `cell.l.display`/富文本 `<r><t>` 含真实 ID。例如 `2026.3!A2=B09PVGGRV1`、`2026.3!I2=B0B2PCVNYJ`、`2026.6!A2=B09YCTD74F`、`2026.6!I2=B0CP3SXDPZ`。此前仅读取 `cell.v` 导致主源月表的 ASIN/父ASIN 被错误写成 NULL，进而无法按父体去重；这不是源数据缺失，而是导入缺陷。2026.01-07 的当前父体快照仍按用户确认的竞品替换链执行，不能与主源行值混称。
- 若在主源 workbook 上直接用 `小类BSR<=100` 筛选后累计，2025 H1 为 `1,249,923`、2026 H1 为 `15,111,465`（约 `+1,109.0%`）；2026.04-06 的命中行数为 885/1,024/1,357，明显不是计划表 `Q=1..100` 的 100 个稳定排名主体，不能用该结果对齐计划 BSR `+5.1%`。
- 为排除“原来的数据”所指对象歧义，另核对了替换前历史的 64-94 父体快照：2026 H1 销量 `1,355,121`、销售额 `$107,709,694`，相对主源 2025 H1 为 `-33.3515%`、`-13.0320%`，同样不能对上计划 BI/BSR 的正向结果；《新增说明情况》中的 202602 `-27.7%/-0.1%` 只是该旧快照的历史校验值。
- 回切判定为“不通过”：将未替换主源 2026 数据直接作为当前主分析源，会得到约 `+707.6%` 的 H1 方向，既不能逐月也不能 H1 对上计划 BI `+2.8%`，BSR Top100 亦不能对上计划表 `+5.1%`。在统计单元、商品范围和字段定义没有新的权威映射前，禁止回切或用正负号修正；继续保留当前父体快照与计划部 BI/BSR 的双轨呈现。

#### 0.5.2 TOP 汇总表与“整体市场”边界澄清（2026-09-04）

- 原始 workbook 的四张 `TOP销量` / `TOP销量（倍率）` / `TOP总销售额` / `TOP平均单价` 是按排名的 Top100（部分还带 Top300 总计）汇总，服务于“小类前100、头部/中部/尾部、五档分层”分析；它们不包含全量市场的所有 Listing，不能替代整体市场月度明细汇总。
- 原始要求把“整体市场销售及销售额、均价”与“小类前100同比环比”列为两个独立分析项。整体市场应从每月明细表按 `月销量`、`月销售额($)` 和价格字段汇总；Top100 应从排名 1-100 的 TOP/BSR 数据单独求和。若直接把 TOP100 叫作整体市场，会漏掉长尾并混淆统计范围。
- 独立重算证据：原始 `TOP销量` 排名 1-100 的 2025 H1 销量为 `1,199,075`，2026 H1 为 `1,210,853`，按同周期公式为 `+0.9823%`；同期主源月度明细全量为 `2,033,235` 和 `16,421,197`。两者不是同一统计单元，也不等于计划部 BI `+2.8106%`。`TOP总销售额` 排名 1-100 的 H1 为 `$65,632,129 -> $63,926,134.80`（`-2.5993%`）。
- TOP 表中的 `总计（TOP前100）` / `总计` 行在多个 2025/2026 月份为空，不能把空白当成 0；需要对排名 1-100 行独立求和并记录行数。计划部新增 workbook 的 `行业大盘数据` `+2.8%` 是 BI 全类目验收源，`BSR底表-美国` `+5.1%` 是另一套 Q=1..100 月度排名源，均不是主源 workbook 的 TOP 汇总直接计算结果。

#### 0.5.3 XLSX 物理读取完整性抽查（2026-09-04）

- **附件状态**：本轮用户附加的 `C:\Users\Administrator\.codex\attachments\b1d1f60f-4f1c-482f-a670-a1e0bce1af51\pasted-text.txt` 文件长度为 0 字节，未提供可追加核对内容。
- **OOXML 与解析器总量核对**：主源 `data/raw/地垫-卖家精灵市场数据.xlsx` 大小 `44,284,788` 字节，SHA-256 `9CED3B100ECE7D441B45CE8A1BD0EB9D3470854A832F071B02ECC2C6F91F5FAC`。直接读取 55 个 `xl/worksheets/sheet*.xml`，共 `4,321,633` 个 `<c>` 节点，其中含值/内联文本的有效单元格 `3,314,297` 个；SheetJS 解析结果同为 `3,314,297` 个有效单元格，未发现值单元格被截断或落在 `!ref` 之外。`Sheet6` 的 2,782 个 XML 单元格均无值/公式，仅为遗留格式区域，因此按空表跳过是预期行为。
- **随机非替换月表**：从未被替换的 50 张月表中随机抽取 `202407`。原表 `A1:BF1959`（第 1 行标题、第 2 行表头、1,957 条数据、58 列），XML 有 1,959 行、113,622 个 `<c>`、76,410 个有效单元格；SheetJS 与 XML 有效单元格数一致。逐格对照 `market.db.monthly_202407`：1,957 行、58 列、0 个单元格差异；`月销量=369,797`、`月销售额=23,163,525` 与交付 JSON 同月指标一致。
- **随机替换月表复核**：另抽查 `202604` 的替换链：`competitor_809440.db.dedup_202604` 与 `market.db.monthly_202604` 均为 1,744 个唯一父体键、64 个共同业务列，逐键逐列 0 个差异；销量均为 `299,007`，销售额均为 `15,410,480`。该值与原始主源 `2026.4!A1:BL2001` 不同，是用户确认的竞品父体快照替换结果，不是 XLSX 漏读。
- **公式缓存缺口**：四张 TOP 汇总表的 OOXML 中共有 `3,554` 个公式单元格，但均没有 `<v>` 缓存值（`TOP销量` 150、`TOP销量（倍率）` 554、`TOP总销售额` 50、`TOP平均单价` 2,800）。原导入器使用 `cellFormula:false` 且仅取 `cell.v`，因此这些公式结果会被写成 NULL；之前的逐格审计只证明“NULL 被一致导入”，没有证明公式结果完整。这是 TOP 汇总表的真实处理缺口，不能继续宣称四张 TOP 表完全无损。
- **修正状态（2026-09-04）**：`src/import_xlsx.js` 已在导入前按原公式依赖重算并写入 TOP 的组别、合计、倍率和平均单价；`tests/full_audit.js` 新增 10 项公式非空检查及 3,354 个数值结果核对，当前均通过。历史缺缓存只说明原始文件没有计算缓存，不再导致当前数据库 NULL。
- **阶段结论**：月度明细没有被 XLSX 截断；TOP 公式结果此前确实未被读取/重算，现已修正并纳入回归审计。当前 2026.01-07 与主源原始明细的差异另来自已登记的父体快照替换和去重边界。任何重新回切或重新发布仍必须先更新本 SPEC 并重新生成、审计、发布。

#### 0.5.4 富文本/超链接 ASIN 读取修正（2026-09-04）

- **用户截图核实**：主源及参考 workbook 的 ASIN、父ASIN单元格使用 Excel 富文本超链接。以 `2026.3!A2` 为例，`cell.v` 为空，但 `cell.l.display` 和 `<r><t>` 为 `B09PVGGRV1`；同一行 `I2` 为 `B0B2PCVNYJ`。截图中的 `B09YCTD74F`、`B091YMYV7Q`、`B0BNBTFNLX` 等均为真实 ASIN，不是说明文字。
- **根因**：旧导入器只读取 `cell.v`，因此把富文本 ASIN/父ASIN整列写成 NULL；这会使 2022-2025 的行级数据无法按父体复核，也会使 PP/GENIMO 的 Listing 去重与领导参考表偏离。此前“主源无 ASIN”及“2026.03 唯一非空值是说明文字”的判断均作废。
- **修正规则**：`src/import_xlsx.js` 与 `tests/full_audit.js` 统一使用 `cell.v` → `cell.l.display` → 富文本 `<r><t>` → `cell.w` 的显示值回退链，并解码 XML 实体；数字、空值和 TOP 无缓存公式仍按原规则处理。
- **修正后抽查**：主源 2025.01 至 2025.06 已分别恢复 1,718/1,683/2,000/2,000/2,000/1,888 个有效 ASIN，父ASIN唯一数分别为 1,644/1,568/1,859/1,813/1,311/1,701；2026.01-07 继续以已确认竞品父体快照替换链为准。`npm run audit` 当前 54 张业务表、4,261,173 个单元格 0 差异。
- **交付影响**：重新导入后，2025 PP/GENIMO 的父体识别和独立 Listing 明细已恢复；所有受影响的 JSON/Markdown/HTML 必须重新生成并复审。富文本修正完成前生成的报告不得继续交付。

#### 0.6 公式版结果工作簿（2026-09-09）

- **交付文件**：`outputs/20260909-formula-market-analysis/户外地垫市场分析-公式版-20260909.xlsx`。这是面向领导验收的单一结果工作簿；参考 workbook 只作为独立对照，不作为本项目明细的验收真值。生成器为 `src/build_formula_market_xlsx.mjs`，运行时需使用工作区配置的 bundled Node 与 `@oai/artifact-tool`。
- **结果页**：`00_概览`、`01_整体月度`、`02_BSR整体`、`03_BSR头中尾`、`04_分类月度`、`05_品牌份额`、`06_决策建议`、`07_核心汇总`、`08_校验`；`90_输入_明细`保留可追溯值，`91_聚合输入`保留由当前明细和同一规则产生的中间聚合，`92_来源与规则`登记源文件、替换、去重、BSR、分类和均价口径。
- **公式边界**：结果页的销量、销售额、均价、覆盖率、MOM/环比、份额、Top100及头中尾均由 Excel 公式计算；明细值和中间聚合是输入值，不写回 `data/raw/` 或 `market.db`。工作簿中核验到 3,625 个公式单元格，打开 Excel 后可重算。
- **时间范围**：为保证 Excel 可用的重算速度，工作簿明细页包含 `2025.01-12`、`2026.01-07`；核心结论仍严格使用 `2026.01-06`，2026.07 只展示。完整 `2022.06-2026.07` 历史数据继续保存在 `market.db`、JSON、Markdown 和 HTML 中。
- **关键校验**：当前公式重算得到整体 2026H1 销量 `1,737,288`、PP `984,309`、高客单非PP `752,979`，两类加总差额为 `0`；销售额加总差额为 `0`；每类别每月 Top100 最大行数为 `100`；核心明细行数为 `9,365`；公式错误扫描无 `#REF!/#DIV/0!/#VALUE!/#NAME?/#N/A`。
- **品牌份额**：`05_品牌份额` 的主指标是 GENIMO ÷ 整体市场，PP 内份额单列为辅助指标；不能把品牌占比限制在 PP 子集。
- **参考基准显示**：`00_概览` 另列计划部 BI 全类目 `+2.8106%` 与计划部 BSR Top100 `+5.0910%`，值按各自来源的分子、分母和公式保留；不得用这两项覆盖 market.db 的不可比明细负向结果。

## 1. 外部资源与依赖清单 (Prerequisites)

### 数据源
- 文件: data/raw/地垫-卖家精灵市场数据.xlsx (44 MB)
- 来源: 飞书多维表格导出 / 卖家精灵市场数据
- 站点: 美国站 (Competitor-US-)
- 时间跨度: 2022.06 - 2026.07
- 工作簿内部共登记 55 张工作表，但 Excel 界面只显示 23 张：
  - 可见表 23 张：4 张 TOP 汇总 + 19 张月度明细（2025.01 - 2026.07）
  - 隐藏表 32 张：31 张月度历史明细（2022.06 - 2024.12）+ 1 张无有效区域的遗留表 `Sheet6`
- 有效业务数据共 54 张表：4 张 TOP 汇总 + 50 张月度明细。`Sheet6` 不属于业务数据，不应称为“1 张空业务表”。
- 原始需求要求分析“历年”数据并计算年度/月度 YoY、MoM，因此导入范围必须包含 31 张隐藏历史明细；工作表是否隐藏只属于 Excel 展示属性，不作为排除数据的条件。

### 运行环境
- Node.js >= 22.5 (已验证 v24.15.0, 内建 node:sqlite)
- 无需 Python 或数据库服务端

### 核心依赖包
- xlsx (SheetJS): 解析 .xlsx
- node:sqlite (Node.js >= 22.5 内建): 写入 SQLite (同步 API, 零原生依赖)
- dotenv: 加载 .env

### 1.1 业务目标与新增要求（必须实现）

本项目不只是 Excel 转 SQLite，还必须从历年户外地垫数据生成可复核的市场分析和 2027 年经营建议。以下要求来自：

- `要求内容.txt`：原始业务要求；
- `新增参考的材料和内容/新增说明情况.txt`：2026-08-28 新增口径及修正要求；
- `新增参考的材料和内容/PP管数据-plastic-2025年-2026年.xlsx`：PP/plastic 月度、年度、BSR 前 100 和预测参考；
- `新增参考的材料和内容/PP塑料户外地垫_BSR前100年度分层与2027链接规划 (1).xlsx`：BSR 分层、父体进退层和 GENIMO 2027 规划参考。

必须交付以下四类分析：

1. **整体市场**：按月、按年度汇总销量、销售额、均价；输出用户定义的月度 MOM/环比（当前月 vs 去年同月）和年度 YoY；不输出本月 vs 上月的连续环比。
2. **PP/plastic 产品线**：商品标题按不区分大小写的完整单词 `plastic` 筛选；汇总销量、销售额、均价；输出月度/年度趋势、BSR 前 100、头部/中部/尾部及五档分层分析；分析 PP 塑料地垫趋势。
3. **高客单非 PP 产品线**：排除 PP 后，**其余全部产品都归入高客单非 PP**；丙纶、三明治等只是参考示例，不再作为额外筛选条件。输出销量、销售额、均价、趋势、BSR 前 100及分层分析，并给出高客单地垫趋势判断。
4. **GENIMO**：输出品牌指标、主力 Listing/父体识别、BSR 分层表现，并给出对 2027 年有实际意义的链接组合、库存、广告、利润和晋级/退出建议。

BSR 前 100 是核心分析范围：排名 100 之后的数据只保留作原始数据和审计用途，不进入核心趋势结论。每个类别、每个月的 BSR 前 100 独立 Listing 数必须不超过 100。

### 1.2 时间、指标和口径优先级

- 原始数据和参考材料保留到 2026.07，核心同比/环比统计及趋势结论至少统计到 2026.06，核心 `ANALYSIS_CUTOFF` 为 `202606`；2026.07 只能作为原始明细/附录或经明确标注的参考，不得静默延长核心结论周期。
- 用户在本项目中对缩写的定义优先于通用财务术语：
  - 月度 **MOM/环比**：今年 X 月 vs 去年 X 月同月，例如 2025.01 vs 2024.01、2026.02 vs 2025.02；BSR Top100 月度汇总也必须按同月比较，例如 2024.05 vs 2023.05。
  - 本月 vs 上月的连续环比不属于当前交付指标，不得在报告、HTML 或结构化 JSON 中另列。
  - 年度 **YoY**：今年与去年相同月份集合的累计比较；2026 年核心年度比较使用 2026.01-06 vs 2025.01-06。
- 每个指标必须保留跨年同月比较基准月份/周期字段（如 `momBasis`、`comparison`），避免只展示百分比而无法复核；`chainBasis/chain*` 为历史字段，不得作为当前交付指标输出。
- 月份标签必须使用可排序且无歧义的格式：内部统一 `YYYYMM`，展示时十月必须写作 `2025.10`，不得因 Excel 数值格式显示成 `2025.1`；月份顺序和缺失月份必须在审计中检查。
- 均价至少输出：SKU/Listing 平均标价，以及按同时具备销量和销售额的记录计算的配对销售额 ÷ 配对销量加权成交均价；两者不得混称为同一均价。平均标价只统计非空、可解析的价格，销量/销售额缺失值不得通过 `Number(null)` 等方式按 0 计入；分析结果必须提供销量、销售额和配对均价的值计数、缺失计数、覆盖率及完整性标记。
- 《新增说明情况》曾给出旧 64-94 父体数据下的 202602 校验值：跨年同月销量约 `-27.7%`、销售额约 `-0.1%`；同一历史文本还记录了上月比较 `+9.3%`、`+33.5%`，仅作为历史证据保留，不属于当前交付指标。
- 当前用户已替换 2026.01-06 数据，因此当前 202602 验收值为跨年同月 MOM/环比；精确计算值见第 6 章验收 20。2026.01-06 为 1038-1993 个父商品/月；主源 2025 月表同样含富文本 ASIN/父ASIN，但其行级展开与 2026 父体快照仍不是同一统计单元，跨年结果只作方向性参考，必须披露统计单元差异。
- 趋势结论、首页指标卡、GENIMO 份额、主力父体和量化建议的历史实绩基线必须统一使用 `2026.01-06`；2025 只能作为同周期比较基准。GENIMO 累计 Top 父体不得混入 2025 或 2026.07 数据。

新增说明逐条落实关系如下：

| 新增说明 | 本 SPEC 的落实位置 |
|---|---|
| BSR Top100 月度汇总的同月比较，例如 2024.05 vs 2023.05 | 1.2、7.4、7.6、验收 18 |
| MOM/环比/YoY 的用户定义 | 1.2、7.4、验收 18 |
| 202602 基准值、2026 口径修正、统计到 2026.06 | 1.2、7.3、7.7、验收 19-21 |
| 数据替换必须登记；业务公式不变，当前数值基准随用户确认的数据源更新 | 0、0.1、7.3、验收 20-21 |
| BSR 前100优先，100名后不作为参考 | 1.1、7.6、7.8、验收 16 |
| 趋势结论和 GENIMO 建议服务 2027 | 1.1、1.3、7.7、验收 22 |
| 2026.01-07 整体市场趋势合并展示，7月跨口径禁算 | 1.2、7.3、7.7、验收 25 |
| 趋势与 GENIMO 量化建议统一使用 2026.01-06 实绩 | 1.2、7.7、验收 26-27 |
| 排除 PP 后其余全部归入高客单 | 1.1、7.5、验收 15 |
| BSR Top100 每月 SKU/Listing 数最多 100 | 1.1、7.6、验收 16 |
| 其他展示数据先核查后修正 | 7.7、验收 21 |
| 前端数据不得缺失 | 7.7、验收 28 |
| 交付前检查数据是否正常、分析是否准确，满足实际生产使用 | 6、7.7、验收 29 |

### 1.3 参考材料中的 2027 规划基准（预测，不是历史实绩）

参考 workbook 提供了 2026.09-12 的市场预测和 2027 年规划基准。它们必须与历史观测数据分开，并在报告中标注为“预测/假设”，不能硬编码成实际销量或销售额：

| 月份 | 销量基准预测 | 销量可能区间 | 销售额基准预测 | 阶段 |
|---|---:|---:|---:|---|
| 2026.09 | 约115,000 | 104,000-125,000 | 约327万美元 | 旺季结束、需求快速回落 |
| 2026.10 | 约99,000 | 89,000-107,000 | 约316万美元 | 淡季+秋季促销 |
| 2026.11 | 约94,000 | 85,000-102,000 | 约387万美元 | 黑五带来销售额修复 |
| 2026.12 | 约101,000 | 91,000-110,000 | 约412万美元 | 低基数+节日场景支撑 |

| 2027 情景 | 年销量 | 年销售额 | 触发条件 |
|---|---:|---:|---|
| 保守 | 168万-172万 | 5700万-5900万美元 | 消费疲软、价格战持续、RV需求下降 |
| 基准 | 177万-181万 | 6200万-6500万美元 | 户外需求稳定、价格逐步企稳 |
| 进取 | 188万-192万 | 6800万-7000万美元 | 春夏天气有利、头部品牌减少价格战、Amazon大促表现良好 |

参考材料的 2027 月度基准预测为：1月 40,500 件/$137万，2月 57,800 件/$203万，3月 130,600 件/$514万，4月 192,500 件/$748万，5月 252,300 件/$815万，6月 300,500 件/$1,003万，7月 225,600 件/$896万，8月 168,600 件/$496万，9月 120,000 件/$347万，10月 102,900 件/$335万，11月 97,600 件/$410万，12月 105,000 件/$437万。上述数值仅作为参考假设和复核材料；如程序重新估计，必须保留新旧假设的来源和差异。

## 2. 配置说明

代码中读取 .env 必须严格使用以下变量名:

| 变量名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| RAW_EXCEL_PATH | string | 是 | Excel 源文件路径 |
| DATABASE_URL | string | 是 | SQLite URL, 形如 sqlite:///data/processed/market.db |
| IMPORT_OVERWRITE | bool | 否 | 是否覆盖已有 DB, 默认 true |
| LOG_LEVEL | string | 否 | debug/info/warn/error |
| ANALYSIS_DB_PATH | string | 否 | 分析生成器读取的 SQLite 路径 |
| ANALYSIS_CUTOFF | YYYYMM | 否 | 核心比较、累计和结论截止月份，默认 202606；202607 可并入整体趋势展示，但跨口径同比/环比/累计必须禁算 |

## 3. 数据结构 (Schema)

### 表命名规则

- 月度明细表: monthly_<YYYYMM>, 例如 monthly_202206
- TOP 销量表: top_sales_volume
- TOP 销量倍率表: top_sales_volume_ratio
- TOP 总销售额表: top_total_sales
- TOP 平均单价表: top_avg_price
- 元数据表: meta (记录每次导入的元信息)
- 工作表目录: sheet_catalog (记录工作表顺序、可见性、有效区域、分类、目标表、导入行数和跳过原因)

### 工作表范围与可见性口径

- SheetJS 的 `wb.SheetNames` 会同时返回可见表和隐藏表，不能将其长度解释为 Excel 界面可见子表数。
- 可见性读取自 `wb.Workbook.Sheets[].Hidden`：`0` 为可见，`1` 为隐藏，`2` 为深度隐藏。
- 当前源文件统计：`total_sheets=55`、可见 23、隐藏 32、有效导入 54、跳过无有效区域 1。
- 转换器应保留全部有效业务表；验证和文档必须分别报告“工作簿内部表数”“界面可见表数”“隐藏表数”“有效导入表数”。
- 空表判断必须依据工作表是否存在有效区域 `!ref`，不能只依赖固定名称 `Sheet6`。

### 通用列规则

- 第一列固定为 row_id (INTEGER, 自增主键), 保证顺序
- 第二列固定为 month_label (TEXT), 规范化月份标签
- 原始 Excel 中的列名直接转 SQLite 列名, 特殊字符替换为下划线
- 所有列类型保持 SQLite 亲和类型 (TEXT/INTEGER/REAL)
- 空值统一写 NULL
- 启用 PRAGMA journal_mode=WAL 提高并发读

### 月度明细表统一字段

| 列名 | 类型 | 含义 |
|---|---|---|
| row_id | INTEGER PK | 行号 |
| month_label | TEXT | 月份标签 |
| ASIN | TEXT | 商品 ASIN |
| SKU | TEXT | SKU/规格 |
| 详细参数 | TEXT | 详细参数 |
| 品牌 | TEXT | 品牌名 |
| 品牌链接 | TEXT | 品牌搜索链接 |
| 商品标题 | TEXT | 商品标题 |
| 商品详情页链接 | TEXT | 商品链接 |
| 商品主图 | TEXT | 主图 URL |
| 父ASIN | TEXT | 父 ASIN |
| 类目路径 | TEXT | 类目路径 |
| 大类目 | TEXT | 大类目名称 |
| 标签 | TEXT | 标签 (仅 2026.3+ sheet) |
| 大类BSR | INTEGER | 大类 BSR 排名 |
| 大类BSR增长数 | INTEGER | 大类 BSR 增长数 |
| 大类BSR增长率 | REAL | 大类 BSR 增长率 |
| 小类目 | TEXT | 小类目 |
| 小类BSR | INTEGER | 小类 BSR 排名 |
| 月销量 | INTEGER | 月销量 |
| 月销量增长率 | REAL | 月销量增长率 |
| 月销售额 | REAL | 月销售额($) |
| 月销售额增长率 | TEXT | 月销售额增长率 |
| 子体销量 | INTEGER | 子体销量 |
| 子体销售额 | REAL | 子体销售额($) |
| 变体数 | INTEGER | 变体数 |
| 价格 | REAL | 价格($) |
| prime价格 | REAL | prime 价格($) |
| Coupon | TEXT | Coupon |
| Q_A | INTEGER | Q&A 数 |
| 评分数 | INTEGER | 评分数 |
| 月新增评分数 | INTEGER | 月新增评分数 |
| 评分 | REAL | 评分 |
| 留评率 | REAL | 留评率 |
| FBA | REAL | FBA($) |
| 毛利率 | REAL | 毛利率 |
| 评级 | TEXT | 评级 |
| 上架时间 | TEXT | 上架时间 |
| 上架天数 | INTEGER | 上架天数 |
| 配送方式 | TEXT | 配送方式 |
| 买家运费 | REAL | 买家运费 (仅 2025.6+) |
| LQS | INTEGER | LQS |
| 卖家数 | INTEGER | 卖家数 |
| BuyBox卖家 | TEXT | BuyBox 卖家 |
| BuyBox类型 | TEXT | BuyBox 类型 |
| 卖家所属地 | TEXT | 卖家所属地 |
| 卖家信息 | TEXT | 卖家信息 |
| 卖家首页 | TEXT | 卖家首页 |
| Best_Seller标识 | TEXT | Best Seller |
| Amazon_Choice | TEXT | Amazon's Choice |
| New_Release标识 | TEXT | New Release |
| A页面 | TEXT | A+ 页面 |
| 视频介绍 | TEXT | 视频介绍 |
| SP广告 | TEXT | SP 广告 |
| 品牌故事 | TEXT | 品牌故事 |
| 品牌广告 | TEXT | 品牌广告 |
| 7天促销 | TEXT | 7天促销 |
| AC关键词 | TEXT | AC 关键词 |
| 商品重量 | TEXT | 商品重量 |
| 商品重量_单位换算 | TEXT | 商品重量换算 |
| 商品尺寸 | TEXT | 商品尺寸 |
| 商品尺寸_单位换算 | TEXT | 商品尺寸换算 |
| 包装重量 | TEXT | 包装重量 (仅 2026.3+) |
| 包装重量_单位换算 | TEXT | 包装重量换算 |
| 包装尺寸 | TEXT | 包装尺寸 |
| 包装尺寸_单位换算 | TEXT | 包装尺寸换算 |
| 包装尺寸分段 | TEXT | 包装尺寸分段 (仅 2026.3+) |

> 旧 sheet 中 重量/体积 与新 sheet 的 商品重量/商品尺寸 含义相同, 统一列名.
> 文本中含括号/$/特殊字符的列名一律替换为下划线.
> Q&A 中的 & 转为下划线 (Q_A).
> Amazon's Choice 中的撇号转为下划线 (Amazon_Choice).

### TOP 汇总表 (4 张) 结构相同

| 列名 | 类型 | 含义 |
|---|---|---|
| row_id | INTEGER PK | 行号 |
| month_label | TEXT | sheet 名 |
| rank | INTEGER | 排名 |
| group | TEXT | 组别 (仅 TOP销量 有) |
| <YYYYMM> 系列 | INTEGER | 每月销量/销售额 |

例: top_sales_volume 表第 1 行: rank=1, group=, 202206=10329, 202207=13063, ...

### meta 元数据表

| 列名 | 类型 | 含义 |
|---|---|---|
| id | INTEGER PK AUTOINCREMENT | 导入批次 ID |
| imported_at | TEXT | ISO 时间戳 |
| source_file | TEXT | 源 Excel 文件名 |
| source_size_bytes | INTEGER | 源文件字节数 |
| total_sheets | INTEGER | 工作簿内部登记的 sheet 总数（含隐藏及无有效区域工作表） |
| monthly_tables | INTEGER | 月度表数 |
| summary_tables | INTEGER | 汇总表数 |
| total_rows | INTEGER | 总记录行数 |
| db_size_bytes | INTEGER | 生成的 DB 字节数 |
| schema_version | TEXT | schema 版本 |

### sheet_catalog 工作表目录

| 列名 | 类型 | 含义 |
|---|---|---|
| sheet_order | INTEGER PK | 源工作簿顺序（1-based） |
| sheet_name | TEXT | 源工作表名称 |
| visibility | TEXT | visible / hidden / very_hidden |
| hidden_code | INTEGER | SheetJS Hidden 原始代码 |
| sheet_ref | TEXT | Excel 有效区域 `!ref` |
| classification | TEXT | monthly / top / empty |
| target_table | TEXT | 目标业务表；跳过时为 NULL |
| imported_rows | INTEGER | 实际导入行数 |
| skip_reason | TEXT | 跳过原因 |

### 3.1 原始 workbook 表级结构登记

本项目存在三类原始 workbook，必须分别核对，不能把参考/计划表误当成 `market.db` 主源：

#### A. 主源 `地垫-卖家精灵市场数据.xlsx`

- 工作簿内部 55 张 sheet：4 张 TOP 汇总、50 张月度明细、1 张无有效区域的 `Sheet6`；有效导入 54 张。
- 4 张 TOP 汇总均为首行表头，映射为 `top_sales_volume`（`TOP销量 `）、`top_sales_volume_ratio`（`TOP销量 （倍率）`）、`top_total_sales`（`TOP总销售额`）、`top_avg_price`（`TOP平均单价`）。TOP 表可能存在空表头但有数据的列，必须保留并按 `_col_<index>` 命名。
- 50 张月度明细的表头通过内容识别（含 `品牌`、`商品标题`、`月销量`），数据从表头下一行开始；名称统一为 `YYYYMM`：2022.06-2024.12 使用 `YYYYMM` 数字名，2025.01-2026.07 可能使用 `YYYY.M`/`YYYY.MM`，均映射为 `monthly_YYYYMM`。
- 月度明细的有效区域随源表变化：2022.06-2025.05 常见为 `A:BF`，2025.06-2026.02 常见为 `A:BK`，2026.03-2026.07 常见为 `A:BL`；不能按固定列号读取，必须按表头和 `!ref` 动态映射。
- 2022.06-2024.12 的 31 张月度表为隐藏表但仍是历年基线；隐藏属性不能作为排除条件。
- 2026.01-2026.07 的主源表行值已被用户确认的竞品快照替换到 `market.db`；这些月份与主 Excel 原始值的差异必须通过 `analysis_replacements`、竞品 raw/dedup 表和哈希留痕，不能把替换后的值伪装成主 Excel 原值。

#### B. 2026 竞品快照 `Competitor-US-YYYYMM-<批次>.xlsx`

- 每个文件 2 张 sheet：`Competitor-US-YYYYMM` 数据表和 `Notes` 说明表；数据表首行为表头、共 65 列，核心列依次包括 `ASIN`、`SKU`、`详细参数`、`品牌`、`品牌链接`、`商品标题`、`商品详情页链接`、`商品主图`、`父ASIN`、`类目路径`、`大类目`、`大类BSR`、`大类BSR增长数`、`大类BSR增长率`、`小类目`、`小类BSR`、`月销量`、`销量环比增长率`、`销量同比增长率`、`月销售额($)` 等。
- 当前 `raw_YYYYMM` 行数（数据库镜像，含表头之后的源数据行/空尾行）与有效键行数/去重行数为：202601 `1135/1134/1134`、202602 `1039/1038/1038`、202603 `2002/2001/1766`、202604 `1745/1744/1744`、202605 `1691/1690/1690`、202606 `2002/2001/1993`、202607 `3000/3000/94`。原始 Excel 的 `!ref` 可能比非空行多 1 行，审计必须以 `raw_YYYYMM` 镜像、键覆盖和去重结果分别记录，不能把 `!ref` 末行直接当成有效商品数。
- 2026.01-06 为用户替换的全市场父体级快照；2026.07 是 3000 行子体级展开、去重后 94 个父体。每月 `raw_YYYYMM` 必须逐单元格镜像原始数据，`dedup_YYYYMM` 必须按第 7.3 节确定性规则生成并记录源行号。
- `data/raw/` 中隐藏的 `~$地垫-卖家精灵市场数据.xlsx`、`~$销量预测计划部底表-户外地垫.xlsx` 是 Excel 锁定临时文件（各 165 bytes），不是数据源；导入、审计和源文件清单必须显式排除 `~$*`。

#### C. 计划部核对 `销量预测计划部底表-户外地垫.xlsx`

该 workbook 不是 `market.db` 主源，但属于本轮必须纳入审计的领导参考/计划模型。共 15 张 sheet（10 可见、5 隐藏），结构如下：

按 0.4.1 的领导验收决策，该 workbook 不进入 SQLite 导入链路；其 `行业大盘数据` H1 销量用于报告的主验收方向（2026 相对 2025 为正），`BSR底表-美国` H1 原始月度汇总用于辅助验收。两项结果必须与 market.db 结果分区呈现并保留完整来源、公式和统计单元说明。

| Sheet | 有效区域 | 状态 | 结构/用途 |
|---|---|---|---|
| `基础销量预测-计划端` | `A1:BH200` | 可见 | 美国/加拿大大盘参数、BSR 系数、产品级 2027 月度保守/正常/乐观预测；参数在 `C2:C4`、`C7:C9`，产品表从第 16 行起 |
| `产品基础信息` | `A1:F200` | 可见 | 站点、父 ASIN、父 MSKU、SPU、核心对标竞品 ASIN、备注 |
| `历史月度销量` | `B1:CQ200` | 可见 | 站点/父 ASIN、历史月销量、2026 月度占比、2027 大盘和自身修正参数 |
| `行业大盘数据` | `A1:AA200` | 可见 | `Outdoor Rugs` 年度总量（2024、2025、2026H1）和 2025/2026 月度大盘销量、2026 月度同比、2027 预测 |
| `竞品数据(参考)` | `A1:V15` | 可见 | 我司父 ASIN 对应竞品销量、2025 合计和 2026H1 合计 |
| `BSR占比` | `A1:AF13` | 隐藏 | 组别 × 2025/2026 月度 BSR 占比/销量汇总 |
| `BSR` | `A1:AV81` | 隐藏 | 排名、组别及 2022.06-2026.07 月度销量 |
| `加拿大` | `A1:T200` | 隐藏 | 加拿大站年度/月度销量、总计、增幅、备注 |
| `BSR目标` | `A1:K14` | 隐藏 | 产品级目标 BSR（月度目标） |
| `大盘趋势` | `A1:Q200` | 隐藏 | 美国站 Outdoor Rugs 年/月销量趋势 |
| `26年实际销量` | `A1:AH200` | 可见 | 2026 实际销量、目标销量、月度实际达成及后续月份计划 |
| `25年实际销量` | `A1:Z25` | 可见 | 2025 实际销量（月度、合计、备注） |
| `BSR底表-美国` | `F1:BQ200` | 可见 | `F:H` 年增幅、`L` 组别、`M:P` 23/24/25/26H1、`Q` 排名、`R` 宽组别、`S:BP` 2022.06-2026.07 月销量 |
| `BSR底表-加拿大` | `E1:AU200` | 可见 | 加拿大 BSR 年增幅、组别、24/25/26H1、排名及 2024-2026 月度销量 |
| `Sheet15` | `A1:C3` | 可见 | 6 月含 PD 销量/PD 销量/实际销量的临时核对项，非主分析表 |

计划 workbook 的计算约束：

- `行业大盘数据!D6` 和 `基础销量预测-计划端!C3` 必须以 `SUM(I4:I9)/SUM(H4:H9)-1` 独立复算；当前值为 `+2.8105838873%`，显示 `+2.8%`。该值是全类目 `Outdoor Rugs` 的 2026H1 同比，不是 PP/plastic 子集同比。
- `基础销量预测-计划端!C2` 为 2025 全年同比 `+5.0340991774%`；`D3=(C3-C2)/C2`，`D4=C3*(1+D3)`，`C4=D4`，因此当前 2027 大盘基准增速约 `+1.5692%`，不可把 C3 的 2.8% 写成 2027 预测结果。
- `BSR底表-美国` 的 H1 汇总应按 `202501-202506`（`AX:BC`）和 `202601-202606`（`BJ:BO`）计算；`202607`（`BP`）不得混入 H1。
- 计划端 BSR 查找必须以实际 `L` 列组别为键、以对应汇总列为返回值。任何指向空的 `A:A` 查找范围、未填充的分组公式、或公式缓存 `0` 均视为未通过审计。

## 4. 技术方案与架构

### 转换流程
```
Excel (.xlsx)
  -> xlsx.readFile() [SheetJS]
  -> 自动识别 header 行 (row 0 或 row 1)
  -> 读取工作表可见性并按内容/名称分类: TOP* / 月度 / 无有效区域 / 未识别
  -> 统一列名 (去括号/$/特殊字符)
  -> 类型推断 (数值列 -> INTEGER/REAL, 其余 -> TEXT)
  -> 批量 INSERT (事务)
SQLite (market.db)
```

### 关键决策
1. 动态 schema: 脚本启动时根据 Excel sheet 动态生成表结构, 避免遗漏新列
2. 批量 INSERT: 单事务 (BEGIN/COMMIT) 包整张 sheet, 速度与稳定性平衡
3. 空值归一: 空字符串/undefined/null 全部写 NULL; 纯空白格视为 NULL
4. header 探测:
   - 月度表: 扫描 row 0 和 row 1, 找到含"品牌"/"商品标题"/"月销量"的行
   - TOP 表: header 在 row 0 (经审计验证)
5. 月份标签规范化: 2025.6 -> 202506, 202501 -> 202501
6. 覆盖策略: `IMPORT_OVERWRITE=true` 时重建 DB；为 `false` 且目标 DB 已存在时必须拒绝执行，不能静默覆盖
7. 空 header 列保留: TOP销量 (倍率) 等表有空 header 但有数据, 自动命名为 `_col_<index>` 保留
8. 隐藏历史表纳入: 31 张隐藏月度表是历年 YoY 的必要基线，不能因隐藏状态而跳过
9. 无有效区域表跳过: 当前 `Sheet6` 没有 `!ref`，不创建业务表、不计入有效导入表数

## 5. 验证过的 API 片段 (PoC)

### 5.1 xlsx 读取
```javascript
const xlsx = require('xlsx');
const wb = xlsx.readFile('data/raw/地垫-卖家精灵市场数据.xlsx', {
  cellDates: false,
  cellNF: false,
  cellFormula: false,
});
console.log(wb.SheetNames.length); // 55（内部总数，不等于界面可见表数 23）
```

### 5.2 node:sqlite 写入 (Node.js >= 22.5 内建)
```javascript
const { DatabaseSync } = require('node:sqlite');
const db = new DatabaseSync('data/processed/market.db');
db.exec('PRAGMA journal_mode = WAL;');
const stmt = db.prepare('INSERT INTO monthly_202206 (asin, brand) VALUES (?, ?)');
db.exec('BEGIN');
for (const r of rowsArray) stmt.run(r.asin, r.brand);
db.exec('COMMIT');
```

## 6. 验收标准

1. 范围完整性: 识别 23 张可见表、32 张隐藏表；导入 50 张月度明细 + 4 张 TOP 汇总；跳过 1 张无 `!ref` 的遗留表
2. 数据完整性: 2026.01-07 之外的月度表及 4 张 TOP 表须与原始 Excel 行数一致；2026.01-07 须与 `competitor_809440.db` 的 `dedup_YYYYMM` 行数和值一致
3. 列完整性: 每个有效 sheet 写入 SQLite 的源数据列数等于 Excel 有效区域列数
4. 类型一致性: 数值列实际为 REAL/INTEGER; 文本列实际为 TEXT
5. 空值归一: 空 cell 全部为 NULL, 无空字符串
6. meta 表: 每次导入写入一条元数据记录，且表数口径不得把 55 解释为可见表数
7. DB 文件: 单文件 data/processed/market.db, 可被任何 SQLite 客户端打开
8. 抽查验证: 至少覆盖隐藏旧月、可见月份、TOP 汇总和最新月份
9. 全量验证: `tests/full_audit.js` 必须按当前有效数据源对 54 张业务表逐行逐列比对；`tests/competitor_audit.js` 必须另行核对 7 份竞品 Excel、`raw_YYYYMM` 和 `dedup_YYYYMM`；不得只在文档中声称通过
10. 覆盖保护: `IMPORT_OVERWRITE=false` 且 DB 已存在时，导入命令必须非零退出且原 DB 不变
11. 值与存储类型分别验收: 源数值与 DB 数值等值不代表 SQLite 存储类型一致；全量审计必须分别报告 value mismatch 与 numeric type change
12. 数据口径异常处置: 2026.03 - 2026.07 源 Excel 为子体级展开口径（销量虚增约 10 倍），已用竞品快照按父 ASIN 去重数据替换（见第 7 章）；替换表须保留列名交集并填充 month_label
13. 需求覆盖完整性: 交付物必须同时包含整体市场、PP/plastic、高客单非 PP、GENIMO 四个类别；每类均有销量、销售额、两种均价、跨年同月 MOM/环比、年度同周期 YoY、BSR 前100及趋势结论
14. PP 筛选验收: 标题必须按不区分大小写的完整单词 `plastic` 筛选；每月按父 ASIN 优先、否则 ASIN 去重，同一 Listing 键只能计一次；2026 父体任一变体命中时该父体归入 PP
15. 高客单验收: 排除 PP 后的所有商品都必须纳入 high 类别；不得因未命中 polypropylene/sandwich/woven/braided 或价格低于 $40 而被排除
16. BSR 前100验收: 每类别每月独立 Listing 数不超过100；只允许使用 BSR 1-100，100 之后不得进入核心分析；头中尾和五档必须复用同一份已截断 Top100 集合，边界与 7.6 一致且各档合计须精确回勾 Top100
17. BSR 精度验收: 每个父体的层级使用其候选变体中可解析的最小小类 BSR；需要保留多值解析标记，并对空值、换行、多值和异常值抽查
18. 公式验收: BSR Top100 月度汇总的 MOM/环比必须按最新用户定义做同月跨年比较（如 2024.05 vs 2023.05、2026.02 vs 2025.02），只展示一个跨年同月基准月份；不得生成本月 vs 上月的连续环比。参考 workbook 使用通用术语把跨年同月比较标为 YoY，执行时按最新用户定义显示为 MOM/环比，不得照抄标签造成错算
19. 截止月份验收: 核心同比/环比、累计和趋势结论至少覆盖至 2026.06；2026.07 不得改变核心截止月份
20. 基准值验收: 当前有效替换数据下，整体市场 202602 必须复现 `skuCount=1038`、`sales=114541`、`revenue=6662322`，MOM/环比基准月 `202502`、销量 `-14.7735%`、销售额 `-20.5049%`（对外显示为 -14.8%、-20.5%）；不得再生成 `202601` 上月基准或 `+9.5530%/+23.8014%` 的连续环比。旧 64-94 父体口径的 -27.7%、-0.1% 及历史文本中的上月比较值仅作历史回归证据，不得要求当前替换数据复现，也不得删除其变更说明
21. 数据修正验收: 发现其他展示数据异常时，必须先回溯源 Excel、数据库口径、父体去重、字段映射和公式；修正后保留修正说明，禁止只改报告文字或硬编码结果
22. 2027 规划验收: 趋势结论和 GENIMO 建议必须服务于 2027 年决策，并覆盖链接组合、尺寸角色、库存、广告、利润和晋级/退出门槛；预测数据必须与历史观测值分开标识
23. 父体进退验收: BSR 前100的 Cohort 必须按父 ASIN 输出留存、退出、新进入及头/中/尾迁移，且每个数量都能回溯到具体比较周期和父体集合
24. 参考材料复核验收: 必须核对两份参考 workbook 的工作表结构、筛选/去重/BSR 公式和 2027 规划；若因 2026 数据源修正导致数值不同，必须在交付物中解释差异，不得默认为程序错误或静默覆盖
25. 整体趋势合并验收: 交付物必须将 2026.01-07 放入同一张整体市场趋势表；2026.01-06 逐月回勾核心 overall 数据，2026.07 逐值回勾源诊断数据，并明确标记 94 父体小样本、`coreComparable=false`，其 MOM/环比基准和值全部为 NULL/不适用；不得同时保留独立 7 月附录趋势造成口径割裂
26. 2026 趋势验收: 四个分类的趋势分析、首页指标卡和总结卡必须使用 2026.01-06 实绩及相对 2025 同期的变化；禁止把 2025 全年指标写成当前趋势结论
27. GENIMO 2026 验收: GENIMO 品牌实绩、PP 内份额、BSR 分层、Top 父体和建议中的量化依据必须使用 2026.01-06；PP 内份额按父体 family-aware 的 PP-only 分母/GENIMO∩PP 分子计算，Top 父体只累计 202601-202606
28. 前端完整性验收: HTML 中所有要求展示的月份、类别、指标、基准月份、口径状态和结论证据不得缺失；前端数值必须可回勾到同次生成的 JSON，空值只能用于业务上确实不适用或源数据缺失的情形，并必须显示原因
29. 生产交付验收: 交付前必须复跑数据导入/替换审计、分析公式审计、前端完整性审计和关键值人工抽查；除明确登记的源数据限制外不得存在未解释异常。结论必须由当前有效数据支撑，达到实际生产使用所需的准确性、完整性和可追溯性后才能标记“可直接交付”


## 7. 后续更新：2026 年数据口径修正与分析规格（2026-08-28，含 2026-08-27 数据修正）

### 7.1 当前有效竞品快照及替换历史（业务公式不变，数值基准更新）

| 文件 | 月份 | 导出批次 | 数据行 | 父体数 |
|---|---|---|---|---|
| Competitor-US-2026.01-842085.xlsx | 2026.01 | 842085 | 1135 | 1134 |
| Competitor-US-2026.02-842080.xlsx | 2026.02 | 842080 | 1039 | 1038 |
| Competitor-US-2026.03-841734.xlsx | 2026.03 | 841734 | 2002 | 1766 |
| Competitor-US-2026.04-841894.xlsx | 2026.04 | 841894 | 1745 | 1744 |
| Competitor-US-2026.05-841906.xlsx | 2026.05 | 841906 | 1691 | 1690 |
| Competitor-US-2026.06-841914.xlsx | 2026.06 | 841914 | 2002 | 1993 |
| Competitor-US-2026.07-809440.xlsx | 2026.07 | 809440 | 3000 | 94 |

结构：单数据 sheet（Competitor-US-YYYYMM）+ Notes；65 列商品明细（ASIN/SKU/父ASIN/品牌/商品标题/大类BSR/小类BSR/月销量/月销售额/价格 等）。2026.01-06 为全市场父体级导出（每父体一行代表子体，小类BSR 覆盖 1-40 万+）；2026.07 仍为子体级展开（每 ASIN 一行、3000 行封顶）。源表尾部可能含全空行，导入时镜像保留（全 NULL 行），不计入独立 Listing。

### 7.2 新数据库：data/processed/competitor_809440.db（由 src/build_competitor_db.js 可复跑重建）

- `raw_YYYYMM`：源快照逐行镜像导入（含月销量/月销售额，行数与 Excel 一致，尾部全空行保留为全 NULL 行）
- `dedup_YYYYMM`：按父 ASIN 去重（同一父 ASIN 多子体只保留 1 条；无父 ASIN 按自身保留）。**确定性规则：每个 Listing 键先取最小可解析小类 BSR；同名次优先月销量/月销售额字段完整行，再按源表顺序稳定决胜**。重放必须逐值复用该快照，不得随机抽样；`tests/competitor_audit.js` 必须逐父体证明代表行既是源表精确行，又符合该规则。
- 分析层独立 Listing 键：父 ASIN 非空时使用父 ASIN，否则使用 ASIN。每月同一 Listing 键只能计一次，禁止把同父体的子体销量/销售额重复相加。
- 分析采用“两层口径”：销量、销售额、价格等指标来自上述确定性代表行；BSR Top100/分层的分类名次可从同父体且符合当前分类的候选变体中取可解析的最小小类 BSR。禁止汇总同父体子体指标。
- 对 PP/plastic，2026 同父体任一标题命中完整单词 `plastic` 即把父体归入 PP，层级取其 plastic 候选变体最佳小类 BSR；high 是其严格补集。GENIMO 同理按父体任一候选变体的品牌识别并取品牌候选最佳 BSR。原始子体明细须保留供审计。

去重结果（2026-08-28 用户替换后，当前有效）：202601=1134、202602=1038、202603=1766、202604=1744、202605=1690、202606=1993、202607=94 个父商品。它决定当前分析和当前数值验收；旧 64-94 父体结果仅保留为替换前历史记录。

### 7.3 market.db 月度表替换规则（2026 全年）

- `src/apply_competitor_2026.js` 将 `dedup_YYYYMM` 确定性重放到 `monthly_202601` ~ `monthly_202607`（保留目标 schema，DELETE + INSERT，并重置行号）
- 列映射取目标表与 dedup 表列名交集；缺失列（如 月销量增长率、_7天促销）置 NULL；month_label 回填月份
- `analysis_replacements` 必须记录月份、基础 Excel 行数、竞品 raw/dedup 行数、源库 SHA-256、选取说明和应用时间；`sheet_catalog.imported_rows` 更新为当前实际表行数
- 替换版 2026.01-06 为全市场父体级口径（1038-1993 商品/月）；主源 2025 为含富文本 ASIN/父ASIN 的行级/变体展开口径（约 1683-2000 行/月），两者仍非同一统计单元。跨年对比只作方向性参考，不构成严格同口径同比。此处是数据变更记录，不是对原始业务口径的改写。
- 2026.07 可留在库内用于明细、审计和参考材料对照，并按领导要求并入 2026.01-07 整体市场趋势表；本次核心分析、累计与跨年同月 MOM/环比结论仍截止 2026.06。程序和报告必须明确区分“合并展示到 2026.07”和“核心统计截止 2026.06”，7月跨年同月比较/累计禁算。
- 参考 Excel 中 2025.1-2026.7 的 PP 数值是在其自身数据源和去重口径下生成的；2026 年切换竞品快照后，不得无条件声称所有月度总量与参考 Excel 完全相等。只能在相同数据源、相同截止月份、相同筛选、去重和 BSR 解析规则下做逐月校验。

### 7.4 分析口径（用户确认的 B 方案）

| 指标 | 定义 |
|---|---|
| MOM/环比（月度，按最新用户定义） | 今年 X 月 vs 去年 X 月（跨年同月，如 2025.01 vs 2024.01、2026.02 vs 2025.02）；BSR Top100 月度汇总同样按同月计算 |
| 上月比较 | 当前交付不计算、不展示本月 vs 上月的连续环比；历史 `chain` 字段废弃 |
| YoY（年度） | 年度同周期对比（今年 vs 去年同月份集合）；核心 2026 年比较为 2026.01-06 vs 2025.01-06 |
| 同比范围标记 | 年度行输出 `timeComparable/scopeComparable/scopeNote`；2026.01-06 vs 2025.01-06 为同周期（`timeComparable=true`）但不同统计单元（`scopeComparable=false`），scopeNote 必须明确“方向性参考、非严格同口径同比” |

月度表和 BSR Top100 表必须显示：当前月份、MOM/环比基准月份、销量/销售额/均价及跨年同月变化。不得再增加“环比基准月份（上月）”或本月 vs 上月的变化列；缺少跨年同月基准字段，或把上月比较混入当前 MOM/环比，均视为不合格。

### 7.5 分析分类定义（analyze_market.js）

| 类别 | 筛选规则 |
|---|---|
| overall | 全部商品 |
| pp | 商品标题（COALESCE 空串）必须按不区分大小写的完整单词 `plastic` 匹配；不得把 `plastics` 等扩展词误纳入；2026 父体任一候选变体命中即将父体归入 PP |
| high | 排除 PP 父体后的全部商品；不再叠加 polypropylene/sandwich/woven/braided 关键词或 `$40` 价格门槛。丙纶、三明治等仅为该集合中的材质示例 |
| genimo | 品牌（COALESCE 空串）等于 `genimo`（不区分大小写）；2026 按父体任一候选变体识别 |

分类顺序必须是：先按完整单词 `plastic` 判定 PP 父体，再将所有非 PP 父体归入 high；GENIMO 是品牌切片，可与 PP/non-PP 结果交叉展示。空标题按空字符串处理。每月整体必须与 PP + high 在 Listing 数、销量和销售额上精确回勾。

### 7.6 BSR 前 100 分析结构（每类别 × 核心 49 月）

- `bsrTop100`：BSR 1-100 候选按名次、Listing 键和稳定行号确定性排序后最多取 100 条（月度 + 年度）
- `bsrGroups`：头部（1-20）/中部（21-50）/尾部（51-100）（月度 + 年度）
- `bsrSegments`：五档非重叠区间 1-5/6-10/11-20/21-50/51-100（月度 + 年度）。原始需求文字写作 1-5、5-10、10-20、20-50、50-100；执行时必须采用上述不重叠边界，避免 5、10、20、50 被重复计算，并在报告中说明。
- `cohort`：按父 ASIN 统计前100的 Retained（留存）、Exited（退出）、Entered（新进入）以及头/中/尾之间的迁移数量和含义；比较周期必须明确。
- BSR 解析：使用 Outdoor Rugs 小类的源字段 `小类BSR`，支持数值、整数文本和 `N.0` 文本；只保留正整数名次，多值取最小有效名次并保留多值标记，无有效正整数时按缺失处理。解析、去重、筛选顺序必须固定并可复跑，不得按销量重新排名。
- 2026.01-07 的 BSR 最佳名次来自竞品 raw 源的同 Listing 候选变体；核心报告要求 `202601`-`202607` 七个月均有可用竞品名次增强，任一必需月份缺失时分析必须失败，禁止静默退回带 `.0` 解析风险的 market.db 字段。
- BSR 前 100 的独立 Listing 数每月最多 100；排序后按 Listing 键稳定截取 100 条，并列名次保留，因此头/中/尾或五档的记录数可能超过区间宽度。`bsrGroups` 和 `bsrSegments` 必须直接从同一份 `bsrTop100` 月度集合切分并精确回勾；对 100 名之后的商品不生成核心趋势结论，报告必须披露并列策略和最大并列数。
- 每个类别均需输出：前 100 月度销量/销售额/均价及跨年同月 MOM/环比、年度同周期 YoY、头中尾月度/年度、五档月度/年度、各层级的销量/销售额/均价变化及趋势判断；不输出本月 vs 上月的连续环比。
- BSR 前100结论必须直接引用对应周期和层级的销量、销售额、Listing 数、单 Listing 效率或均价变化，不能用没有数据支撑的泛化判断。

### 7.7 分析输出与交付

- `src/analyze_market.js` 从 market.db 生成四份当前交付物（数据全部代码生成，禁止手工改成品）：
  - 交付/户外地垫市场分析数据.json（全量结构化中间结果）
  - 交付/户外地垫市场分析报告-优化版.md
  - 交付/户外地垫市场分析报告-优化版.html（大字版，body 18px/表格 15px）
  - 交付/户外地垫市场分析报告-极速版.md（管理层摘要；口径限制必须与完整报告一致）
- 每类别含：月度指标（销量、销售额、两种均价、跨年同月 MOM/环比）、年度汇总（销量、销售额、两种均价、同周期 YoY+可比列）、BSR前100（汇总/头中尾/五档，月度+年度）、趋势分析（2026.01-06实绩/BSR占比/头中尾同比/202602及核心截止月 MOM/环比/2026核心峰值/口径提示）；2025 只作为同周期比较基准。
- 整体市场还必须在月度明细和 market.db 结果之前显式展示 `leadershipBenchmark`：计划部 BI 全类目 H1 销量（主验收）及计划部 BSR Top100 H1 销量（辅助验收），并同时说明 market.db 负向快照因统计单元不同仅作不可比明细参考；计划表没有销售额/均价时不得补推。
- 整体市场必须先展示 2026.01-07 合并趋势表，再展示核心月度/年度表；合并表包含口径状态列，2026.07 的跨年同月 MOM/环比显示“不适用（口径不同）”。同时包含当前替换数据的 202602 基准核验值、旧基准差异说明、2026 年口径变更记录和 2026.03-06 的数据修正/可比性标记；所有展示数据须经过抽查，发现异常先核查源数据、去重、字段映射和公式，再修正，禁止仅修改展示文本。
- PP/plastic 章节必须包含标题筛选口径、独立 Listing 明细、月度/年度汇总、BSR 前 100 和趋势分析；参考材料中的 `基础月和年度分析`、`只取TOP100分析`、`listing明细` 作为结构和公式校验样例。
- 高客单非 PP 章节必须覆盖排除 PP 后的全部产品，不得只覆盖若干材质关键词或高价商品，并且必须单独给出该集合的销量、销售额、均价和 BSR 分层趋势。
- 结论与行动建议必须面向 2027 年，且量化依据统一取 2026.01-06 实绩，不能把 2025 全年或混合多年累计冒充当前基线。至少包含：市场趋势与风险、PP/非PP组合、尺寸/价格结构、库存与广告节奏、GENIMO 链接组合及可执行的晋级/退出门槛。
- GENIMO 主力父体榜仅累计 2026.01-06，按父 ASIN 优先的独立 Listing 键聚合；GENIMO 在 PP 中的份额必须按 2026 父体 family-aware 分类重算，不能用品牌总量直接除以 PP 总量。
- GENIMO 2027 规划应按“1 个头部锚点 + 3-5 个中部利润层 + 4-8 个尾部测试池”给出建议：头部 1-20、中部 21-50、尾部 51-100；同时包含尺寸角色、销量角色、毛利/TACOS 规则、广告规则和动作结论。参考材料中的尺寸角色为：5x8 流量锚点、6x9 承接升级、8x10/9x12 销售额和利润核心、9x18/10x20 场景型并控制库存，圆形/特殊尺寸只保留验证成功的花型；头部可保留 5x8 Black Gray 主锚点（参考 ASIN B09YCTD74F），中部重点测试 8x10/9x12/10x14 及 Black Beige/Blue Grey，尾部测试新花型和特殊尺寸。
- GENIMO 决策门槛必须纳入：Tail→Middle 连续 4 周 BSR≤100、CVR 达到类目基准、TACOS≤15%、库存覆盖≤90天；Middle→Head 连续 6 周 BSR≤50、贡献毛利≥15%、自然单占比提升且可支撑 60 天补货；头部扩量结算毛利率≥5%、TACOS≤12%，连续亏损 14 天降广告并缩占比 2-3 个百分点；连续 90 天无法进入前100或资源占用高于增量利润则退出。
- GENIMO 的工艺/包装改动建议必须采用小范围验证：取消包边、包装袋或地钉等变更先在低风险颜色或 4x6/5x8 小批量测试，新旧工艺保留批次标记，跟踪散边、卷边、破损和退货原因，至少累计 300-500 单后再决定扩大；8x10、9x12 和超大尺寸暂时保留更稳定的边缘处理。
- 参考材料中的 2026.09-12 趋势预测和 2027 月度基准预测属于 `PP/plastic BSR Top100` 的预测/规划，不得冒充历史观测值，也不得当作整体 market.db 全市场预测；如写入交付物，必须标注“预测/假设”，记录来源工作表、源文件 SHA-256、源单元格范围、假设和可调整参数。参数调整只产生外部重算输入，不回写历史实绩。
- 预测来源固定为 `新增参考的材料和内容/PP管数据-plastic-2025年-2026年.xlsx` 的 `汇总`：2026 Q4 使用 `G3:J6`，2027 月度使用 `R2:T13`，2027 情景使用 `N3:P5`。结构化 JSON 顶层必须输出 `forecastProvenance`，并保留 `scope/sourceFile/sourceSha256/sourceSheet/sourceCells`。
- 报告生成时间必须默认采用 source `meta.imported_at`（可由显式 `REPORT_GENERATED_AT` 覆盖），保证同一导入批次重复生成的 JSON/Markdown/HTML 内容稳定。
- 2025 行级数据中的父体重复只生成敏感性诊断（重复行、首行代表值、最大代表值与行汇总差异）；在业务确认字段是父体汇总还是变体值之前，不得自动把首行或最大值写成历史主口径，也不得据此改写同比结论。
- 线上部署：gh-pages 分支（index.html = 优化版 HTML），GitHub Pages 托管
- 注意：analyze_market.js 的月度 MOM/环比口径必须与 0.3、7.4 一致（当前只允许 `mom=lastYear` 的跨年同月比较；不得恢复或输出 `chain=previous` 的连续环比）；核心统计截止月份必须为 `202606`。

### 7.8 已知限制

- 2026.01-06 为全市场父体级快照（1038-1993 商品/月），主源 2025 年为含富文本 ASIN/父ASIN 的行级导出（1683-2000 行/月，含变体行）；统计单元不同，跨年百分比仅作方向性参考，不能称为严格同比
- 2022-2025 的月度源表也含富文本 ASIN/父ASIN；正确提取后可按父体/ASIN核对，但历史 BSR Top100 仍是按名次排序截取的源表行，不能仅凭行数证明是 100 个独立 Listing；每月必须输出标识覆盖率、不同名次数和重复名次行诊断。2026 为父体/ASIN Listing 池，跨年 BSR 变化同样只作方向性参考。
- 2026.07 仍为 94 父体小口径，BSR 51-100 档数据稀疏；“商品不足100”不得导致把 100 名之后的数据补入前100。
- 2025.05（主表导出日 2025-06-19，ASIN/父ASIN 以富文本超链接保存）源数据存在小类 BSR 同值重复（BSR=17 重复 112 行、23 重复 125 行、58 重复 156 行等，变体行共享父体名次），按名次取前 100 后全部落入 1-20，2025.05 中部 21-50/尾部 51-100 为空 → 2026.05 中部/尾部跨年同月 MOM/环比“无对应数据”；2026.05 头部跨年同月比较的基准为异常 100 行头部，仅作参考。该情形必须显式披露（分层行输出 `momGapReason`，表格显示“无对应数据”并在口径说明中解释），禁止编造或平移基准。
- 需要 2025 年竞品快照或更大范围竞品列表才能做完全同口径跨年对比
- 2026-09-04 富文本复核更正：上一条“2025.05 无 ASIN/父ASIN 列”的文字描述作废；实际 ASIN/父ASIN 以富文本超链接保存，当前限制仅为 BSR 同值与行级/变体展开造成的统计单元差异。
- 2026.07 虽并入整体市场趋势表，但不属于本次核心统计截止月份；2026 年结论须以 2026.01-06 为主，7月必须标记为94父体小样本，禁止计算或解读其跨口径同比、环比和累计。

### 7.9 2026-09-03 原始表/数据库/分析复核基线

- `npm run verify`：128 项通过；54 张有效业务表的行数、`meta`、`sheet_catalog`、替换月份和代表性抽样全部通过。
- `npm run audit`：主源非替换表与 `data/raw/地垫-卖家精灵市场数据.xlsx`、替换表与 `competitor_809440.db` 的 `dedup_YYYYMM` 逐单元格核对，54 张表、4,261,173 个单元格，结构问题 `0`、值差异 `0`；当前基线的 69,027 处仅为 SQLite 数值亲和类型变化，属于信息性告警，不得当作数值一致性通过之外的业务修正。
- `npm run audit:competitor`：7 份 `Competitor-US-YYYYMM-<批次>.xlsx` 与 `raw_YYYYMM` 逐单元格核对，819,910 个单元格、51 项检查，失败 `0`；并单独回归验证 `N.0` 与非零小数解析边界；每月有效键覆盖、父 ASIN 去重行和最佳 BSR 代表行均可回溯到原始精确行。
- `npm run audit:plan-reference`：直接读取 `新增参考的材料和内容/销量预测计划部底表-户外地垫.xlsx`，独立核对行业大盘 H1、计划端 2.8%/2027 基线、美国 BSR Top100 月度输入，并检测原表共享公式覆盖缺口与 P 列月份误用；19 项检查，失败 `0`。该审计只做参考核对，不把计划 workbook 改成 `market.db` 导入源。
- `npm run test:analysis`：重新生成 JSON/Markdown/HTML/极速版，分析审计需覆盖 202602 当前基准、跨年同月 MOM/环比唯一口径、2026.01-06 核心截止、2026.07 禁算、Top100 截断、PP/high 分区、均价和缺失数据披露；同时校验 `N.0` BSR 解析、七个月名次增强 fail-closed、并列名次披露、2025 父体重复敏感性、预测来源指纹、可复现时间戳、GENIMO 跨年值不得作为定量基线以及 `meta.db_size_bytes` 与最终文件一致；不得再验收或生成上一月 `chain` 百分比。
- `npm run test:frontend`：当前基线为 43 张 HTML 数据表、32,795 个单元格与 JSON/常量一致，无 `NaN`、`undefined`、`Infinity` 或缺失关键表；`leadershipBenchmark` 的 +2.8106%/+5.0910% 已登记为顶层可溯源验收基准，并与 market.db 负向明细明确分区。
- 计划部 workbook `销量预测计划部底表-户外地垫.xlsx` 当前没有对应 SQLite 数据库；本轮只能对其 15 张 sheet 做结构、公式和独立重算，不能把它的静态值宣称为 `market.db` 已核对值。其 `2.8%` 已通过月度输入独立复算，但 `BSR底表-美国` 查找范围、H1 公式、月度同比公式和产品行公式存在结构性缺口，未达到可直接交付的预测模型标准。
- 本轮 `analyze_market.js` 直接读取计划部 workbook 生成 `leadershipBenchmark`，只提取可独立复算的 H1 销量基准；该读取不改变 market.db，也不将计划表中的预测/目标或缺失销售额当作历史实绩。
- 2026 替换前后独立汇总（用于排除“导入错位”）：主源 Excel 的 2026.01-07 原始销量依次为 `116,036 / 117,566 / 291,479 / 2,924,738 / 5,387,705 / 7,583,673 / 5,302,834`；当前 `market.db` 依次为 `104,553 / 114,541 / 252,620 / 299,007 / 407,611 / 558,956 / 293,612`，且逐月与 `competitor_809440.db.dedup_YYYYMM` 完全一致。2026.04-06 的主源百万级数值来自源表子体展开，当前较小数值来自用户确认的父体快照替换，不是 SQLite 列错位。
- 计划 BSR 底表独立重算：按 `Q=1..100`，2025H1 `AX:BC=1,066,818`，2026H1 `BJ:BO=1,121,130`，增幅 `+5.0910%`；它既不是行业大盘 `+2.8106%`，也不能直接替代当前报告 BSR Top100 的 `+10.7899%`，因为来源、代表行和统计范围不同。
- 上述审计基线证明的是“当前 DB 对其声明的有效源链路无值丢失”，不证明不同来源之间数值应当相等；计划 BI 大盘、主源卖家精灵库和竞品父体快照的跨源差异必须在报告中单独展示和解释。

### 7.10 2026-09-08 增量修复验收

- BSR 名次解析已修复为正整数语义，`1`、`"1"` 和 `"1.0"` 均解析为 1；非零小数（例如 `"1.5"`）、空值或无正整数结果按缺失处理，多值取最小有效名次并保留标记。导入器、竞品库重建器、竞品审计器和分析器使用同一边界规则。缺少 202601-202607 任一竞品名次增强时，`analyze_market.js` 非零退出。
- 2026 核心 Top100 先筛选 BSR≤100，再按 Listing 键稳定截取 100 条；并列名次不被静默打散，JSON/报告记录最大并列数，解释分层记录数可能超过区间宽度。
- 2025.01-06 父体重复只输出敏感性诊断；在字段语义确认前，首行和最大值均不是历史主口径。该未决项由 `docs/TASKS.md` Task 10.6 跟踪。
- 预测交付范围固定为 `PP/plastic BSR Top100`，来源 `PP管数据-plastic-2025年-2026年.xlsx!汇总`，并记录源文件 SHA-256 和 `G3:J6`、`R2:T13`、`N3:P5` 单元格范围；预测参数属于静态假设，调整需外部重算。
- 报告默认以 `meta.imported_at` 作为生成批次时间；竞品替换脚本在最终 WAL checkpoint 后刷新 `meta.db_size_bytes`，两者均由分析审计回归验证。当前最新全套验收：`npm test` 通过（128 项快速验收、全表 54 张/4,261,173 单元格、竞品 51 项、计划参考 19 项、分析 101 项、前端 32 项、覆盖保护通过）。


## 8. 数据文件路径全景（2026-08-28 全量核对）

### 8.1 原始数据（data/raw/）

| 文件 | 大小 | 用途 |
|---|---|---|
| data/raw/地垫-卖家精灵市场数据.xlsx | 42.23 MB | 主数据源：50 张月度表（2022.06-2026.07）+ 4 张 TOP 汇总；2026.03 起为子体级展开口径（见 7.3） |
| data/raw/Competitor-US-2026.01-842085.xlsx | 0.69 MB | 竞品快照 2026.01（全市场父体级，1135 行，65 列含父ASIN） |
| data/raw/Competitor-US-2026.02-842080.xlsx | 0.64 MB | 竞品快照 2026.02（全市场父体级，1039 行） |
| data/raw/Competitor-US-2026.03-841734.xlsx | 1.10 MB | 竞品快照 2026.03（全市场父体级，2002 行） |
| data/raw/Competitor-US-2026.04-841894.xlsx | 1.00 MB | 竞品快照 2026.04（全市场父体级，1745 行） |
| data/raw/Competitor-US-2026.05-841906.xlsx | 0.97 MB | 竞品快照 2026.05（全市场父体级，1691 行） |
| data/raw/Competitor-US-2026.06-841914.xlsx | 1.14 MB | 竞品快照 2026.06（全市场父体级，2002 行） |
| data/raw/Competitor-US-2026.07-809440.xlsx | 1.29 MB | 竞品快照 2026.07（子体级 3000 行、94 父体；并入整体趋势展示，跨口径指标禁算） |
| 新增参考的材料和内容/销量预测计划部底表-户外地垫.xlsx | 0.24 MB | 计划部/领导提供的销量预测核对 workbook；15 张表，含 BI 大盘、美国/加拿大 BSR 底表、产品历史销量和 2027 预测；不是 `market.db` 自动导入源 |

> 竞品快照结构：单数据 sheet（Competitor-US-YYYYMM）+ Notes；65 列商品明细（父ASIN/品牌/商品标题/大类BSR/小类BSR/月销量/月销售额/价格 等）。2026.01-06 为全市场父体级导出（每父体一行代表子体）；2026.07 为子体级展开（每 ASIN 一行）。2026-08-28 由用户替换更新。

### 8.2 处理数据库（data/processed/）

| 文件 | 大小 | 用途 |
|---|---|---|
| data/processed/market.db | ~50 MB | 主分析库：monthly_YYYYMM × 50 + top_* × 4 + meta + sheet_catalog；2026.01-06 月度表已替换为全市场父体级快照，2026.07 替换为 94 父体快照 |
| data/processed/competitor_809440.db | ~25 MB | 竞品快照库：raw_YYYYMM（源表镜像）+ dedup_YYYYMM（父ASIN去重代表行，由 src/build_competitor_db.js 可复跑重建） |
| data/processed/competitor.db | 2.35 MB | 早期竞品对比库（808036 批次，2026.07 单月，历史遗留） |
| data/processed/market.db-shm / -wal | - | SQLite WAL 附属文件（运行时生成） |

### 8.3 参考材料（新增参考的材料和内容/）

| 文件 | 大小 | 内容 | 状态 |
|---|---|---|---|
| 新增参考的材料和内容/PP管数据-plastic-2025年-2026年.xlsx | 7.88 MB | 领导提供并确认通过的 PP 管分析参考：23 sheets（汇总含 2026.09-12 预测与 2027 预测、基础月和年度分析、只取TOP100分析、listing明细、2025.1-2026.7 月度） | 已确认参考材料，未纳入程序管线 |
| 新增参考的材料和内容/PP塑料户外地垫_BSR前100年度分层与2027链接规划 (1).xlsx | 0.02 MB | 领导提供并确认通过的 BSR 前100 战略分层参考：6 sheets（Dashboard 结论、Tier 年度分层、Monthly 前100趋势、Cohort 进退层、GENIMO 2027规划、Notes 口径） | 已确认参考材料，未纳入程序管线 |
| 新增参考的材料和内容/要求内容.txt | 1.2 KB | 需求原文（从项目根目录移入，内容未变） | 需求依据 |
| 新增参考的材料和内容/新增说明情况.txt | 1.6 KB | 新增要求：BSR Top100 同月 MOM 计算、MOM/环比/YoY 定义、202602 历史口径校验值、统计截止 2026.06、BSR100 优先、2027 结论、排除 PP 后全量高客单、Top100≤100、其他数据纠错，以及当前补充的前端无缺失和生产交付前复核 | 必须同步到本 SPEC |

参考材料的关键内容必须在程序和报告中可追溯：

- PP workbook 的 `基础月和年度分析`：标题含 plastic、父 ASIN 优先去重、月度销量/销售额/均价、可比性和异常说明。该表中的跨年同月比较可作为当前“月度环比/MOM”公式样例；不得把参考表的通用术语或本月 vs 上月列引入当前交付。
- PP workbook 的 `只取TOP100分析`：BSR 前100 月度销量/销售额/ASP、头部/中部/尾部销量，以及 2027 GENIMO 链接组合参考。
- PP workbook 的 `listing明细`：独立 Listing 键、代表 ASIN/父 ASIN、代表标题、销量、销售额、价格、BSR、同父体命中数和源数据行。
- 战略 workbook 的 `Dashboard 结论`、`Tier 年度分层`、`Monthly 前100趋势`、`Cohort 进退层`、`GENIMO 2027规划`、`Notes 口径`：BSR 1-20/21-50/51-100 分层、父体进退、2027 链接组合和决策门槛；`Monthly 前100趋势` 的跨年公式（如 2026.02 对 2025.02）同样标为 `YoY`，需按本项目用户定义映射。
- 两份参考 workbook 都是 PP/plastic 分析材料，不是整体市场 202602 四项百分比的来源；整体市场旧基准来自《新增说明情况》及当时 64-94 父体数据，当前基准必须从用户替换后的整体市场数据重新计算。
- 两份参考 workbook 中的数值只能在同一数据源、同一筛选、同一去重和同一 BSR 解析口径下作为校验值；2026 数据源切换后，必须在报告中标注不可直接横向比较的月份。

### 8.4 交付物（交付/）

| 文件 | 大小 | 用途 |
|---|---|---|
| 交付/户外地垫市场分析报告-优化版.html | 约 448 KB | 唯一最终 HTML 报告，含 3 张静态 SVG 趋势图；gh-pages 线上部署即此文件（index.html） |
| 交付/户外地垫市场分析报告-优化版.md | 约 262 KB | 完整 Markdown 版报告 |
| 交付/户外地垫市场分析报告-极速版.md | 约 2 KB | 管理层摘要，保留关键口径限制 |
| 交付/户外地垫市场分析数据.json | 约 1.8 MB | 全量结构化分析数据，含历史 BSR 质量诊断 |
| docs/archive/ | - | 早期报告与旧速览稿归档，不得作为当前交付物 |

### 8.5 程序与配置

| 路径 | 用途 |
|---|---|
| src/import_xlsx.js | 地垫 xlsx → market.db 导入器（全量重建） |
| src/build_competitor_db.js | 从 data/raw 竞品快照可复跑重建 competitor_809440.db（raw_ 镜像 + dedup_ 最佳BSR/字段完整性/源行序代表规则） |
| src/apply_competitor_2026.js | 将竞品库 dedup_YYYYMM 代表行确定性重放到 market.db monthly_202601-07，并写入替换审计元数据 |
| src/analyze_market.js | market.db → JSON/MD/HTML 分析生成器（数据全部代码生成） |
| tests/verify.js、tests/full_audit.js、tests/competitor_audit.js、tests/plan_reference_audit.js、tests/analysis_audit.js、tests/frontend_audit.js、tests/overwrite_guard.js | 两阶段数据、竞品原表、计划参考 workbook、分析公式/回勾、最终HTML逐格溯源和覆盖保护验收；任何预期数据源切换都必须显式分流，正式测试不得保留“预期失败” |
| .env | 路径配置：RAW_EXCEL_PATH、DATABASE_URL、COMPETITOR_DB_PATH、ANALYSIS_DB_PATH；核心 ANALYSIS_CUTOFF=202606，202607 仅并入整体趋势展示且跨口径指标禁算 |

### 8.6 部署路径

- 本地交付：交付/户外地垫市场分析报告-优化版.html
- 线上：gh-pages 分支 index.html（与本地文件 git-hash 一致）→ https://zhouchengye-0914.github.io/t5-outdoor-rug-market-analysis/
- 部署流程：git worktree 隔离目录 → checkout --orphan gh-pages → 复制 index.html → push -f origin <sha>:refs/heads/gh-pages（推送偶发 exit=128，重试即可）
