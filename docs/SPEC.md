# SPEC：户外地垫市场分析与可回勾交付

版本 3.6，2026-09-23。沿用3.5的候选池、父体Q/T平均和MOM/YOY定义；纠正BSR支线：BSR按源行直接划三档，不按父ASIN归并、不计算父体平均BSR；每个BSR档位内的Q列销量和T列销售额对入档源行直接取平均。3.5及以前生成的旧数字和旧交付必须在按本规范重建后才能继续使用。

## 1. 交付与主源

- 四个部分：整体市场、PP市场、高客单价市场、Genimo品牌。Genimo是品牌视角，不是互斥市场。
- 只使用 `data/raw/0920new` 内2024.08—2026.07的24个按月文件。旧总表和参考表不进入本批次计算。
- 原始文件只读；记录文件名、业务Sheet、月份、行数、排除原因和SHA-256。文件月份必须唯一连续；业务Sheet与月份、表头字段及顺序必须匹配。违反时停止构建。
- 交付目录：`outputs/20260920-new-source-parent-model/`。一份公式XLSX，以及共用同一模型的HTML、JSON、Markdown、构建清单。
- 不计算利润；FBA、毛利率、Coupon仅保留原始快照，不参与业务推导。

## 2. 确定候选池

1. `小类目`只作为原始字段和回勾信息保留，不参与候选池筛选；不得因为某一行没有 `Outdoor Rugs` 文本而排除该行。
2. 对“小类BSR”单元格按原始换行拆分并独立解析，不再把“小类目”与“小类BSR”按位置绑定，也不以 `Outdoor Rugs` 名称决定使用哪一个排名。
3. 排名必须为正整数或等值的`.0`文本。一个源行只要存在至少一个1≤排名≤100的可解析小类BSR值，就进入候选池；整行的全部原始排名和解析结果必须保留，不能静默丢弃其他排名。
4. 同一源行包含多个小类BSR时，标记 `MULTI_BSR`。候选筛选只判断是否存在有效1—100排名；全部解析排名保留给独立BSR支线。BSR支线按源行的有效排名直接划档，不按父ASIN归并；Q/T父体主指标不读取BSR支线结果。
5. 一个源行没有任何有效排名时标记 `INVALID_BSR`；有有效排名但没有任何值落在1—100时标记 `OUTSIDE_TOP100`。
6. 同月ASIN重复必须阻止发布，不能直接重复求和。当前数据无重复不代表将来自动忽略此规则。

候选池包含多条子体，因此“BSR前100”不意味着只能保留100行。原始90页保留未进入候选池的行；91页仅保留小类BSR存在至少一个1—100排名的合格候选。

## 3. 父体统计单元

- 分组键：月份+父ASIN。父ASIN缺失依次回退ASIN、源行ID，不跨月去重。
- 原始明细不删除；92页每个父体月份一条汇总。不同指标分别计算，不使用一条代表行代替整个父体。
- 91页按月份、父体键、源月销量、ASIN排序，使同一父体的候选记录连续，便于公式回算；源文件和源行号始终保留。
- 父体Q/T平均值可能涉及源系统定义的整个父体，可能包括BSR不在前100的变体。本文“市场”指BSR合格子体所对应的候选父体样本，不能称为全站真实市场普查。BSR不写入父体主汇总；它只保留在候选明细和独立分层支线。

## 3A. 字段契约与使用范围

源表列位以业务Sheet表头为准：

| 列 | 字段 | 使用范围 |
| --- | --- | --- |
| A | ASIN | 父ASIN缺失时的分组回退键、源行回查 |
| B | SKU | 原始回查，不参与市场主指标 |
| C | 详细参数 | 原始回查，不参与市场主指标 |
| D | 品牌 | Genimo品牌识别和品牌回查 |
| E/G/H | 品牌链接、商品详情页链接、商品主图 | 原始回查，不参与计算 |
| F | 商品标题 | PP/高客单价归类，完整单词`plastic`匹配 |
| I | 父ASIN | 月份+父体主分组键 |
| J/K | 类目路径、大类目 | 原始回查，不参与候选筛选 |
| L/M/N | 大类BSR、大类BSR增长数、大类BSR增长率 | 原始回查，不替代小类BSR |
| O | 小类目 | 原始回查，不作为`Outdoor Rugs`文本筛选条件 |
| P | 小类BSR | 候选池1—100筛选，以及独立BSR源行三档支线 |
| Q | 月销量 | 父体主销量：同月同父ASIN有效值平均 |
| R/S | 销量环比增长率、销量同比增长率 | 原始回查；报告MOM/YOY全部由聚合Q/T重新计算 |
| T | 月销售额($) | 父体主销售额：同月同父ASIN有效值平均 |
| U/V | 子体销量、子体销售额($) | 同时有效时求和，只作子体覆盖和均价诊断 |
| W | 变体数 | 原始回查和数据质量诊断，不参与销量/销售额求和 |
| X | 价格($) | 有效值形成父体平均标价及覆盖诊断，不回填T |
| Y/Z | prime价格($)、Coupon | 原始回查；不参与利润或主销售额推导 |
| 其余字段 | Q&A、评分、FBA、毛利率、广告等 | 仅保留90原始快照，除非本规范另行指定，不参与市场主指标 |

程序另外生成月份、源文件、源Sheet、源行ID、父体键、BSR解析列表、候选状态、`MULTI_BSR`、Q/T有效性和覆盖率等审计字段。

月度和年度报告的固定指标列为：月份、月销量、月销售额($)、平均标价($)、加权成交均价($)、销量MOM、销量YOY、销售额MOM、销售额YOY。业务表不展示父体数量列；父体数量只在审计页作为辅助核对字段。加权成交均价定义为父体T平均销售额合计÷父体Q平均销量合计；平均标价使用父体内有效X列的平均值再按父体做简单平均，两个均价不能混用。BSR支线沿用同一组固定指标列，另外增加一个维度列“BSR档位”。

## 4. 父体月度主指标平均

父体组内的Q列“月销量”和T列“月销售额($)”不相加，分别对有效值做算术平均。同一月份+父ASIN形成一个父体月度统计单元：

```text
父体月销量 = AVG(Q列月销量的有效非负值)
父体月销售额 = AVG(T列月销售额($)的有效非负值)
```

规则：

1. 空白、`-`、不可解析值和负数不进入平均；
2. 原始明确的0是有效值，进入平均；
3. 没有任何有效Q值时，父体月销量留空，状态`NO_VALUE`；
4. 没有任何有效T值时，父体月销售额留空，状态`NO_VALUE`；
5. 有效值只有部分候选行存在时，保留`PARTIAL`和有效行覆盖率；
6. 全部候选行都有有效值时，标记`FULL`；
7. 平均结果保留小数，不静默取整；
8. 同时保留有效值数、不同值数、最小值、最大值和平均值。不同值数大于1时标记`AVERAGE_CONFLICT`，用于提醒业务检查源值差异。

该规则是对源表父体展示值的去重平均，不等同于真实订单成交，也不等同于把子体销量或子体销售额相加。Q/T必须被业务确认是父体级字段；如果供应方实际按子体或价格变体生成Q/T，平均值只能作为“候选行均值”，不能解释为真实父体总量。

## 5. 子体销量、销售额、均价

- 有效子体要求子体销量与子体销售额都为有效非负数值；零销量但正销售额视为无效矛盾。明确0/0保留为零。
- 仅一项有值或者两项均缺失均不进入有效子体统计；缺失不填零。
- 有效子体销量=有效行子体销量之和；有效子体销售额=同一批有效行子体销售额之和。这两个子体字段保留为覆盖和诊断指标，不再替代Q/T父体月度主指标。
- 父体没有有效行时，子体销量、销售额留空，状态 `NONE`；部分候选行有效为 `PARTIAL`；全部候选行有效为 `FULL`。
- 子体覆盖率=有效候选子体行数÷候选子体行数；它不是已采集变体数÷全部变体数。即使FULL也不能称为完整父体真实收入。
- 子体均价=有效子体销售额÷有效子体销量，分母必须>0。父体主指标均价=父体平均T列÷父体平均Q列，父体主指标和子体诊断均价必须分开列示，不能混用。
- T列“月销售额($)”现在作为父体主销售额的平均输入，不再按行累加；同时保留T列最小值、最大值、有效行数、覆盖率和`AVERAGE_CONFLICT`。不以当前价格回填历史金额；隐含售价只作为源字段诊断。
- 部分覆盖金额变化只能称为已观测子体金额变化。分析必须同时列示本期与基期覆盖率，不能单独据此断言完整市场增长。

## 6. PP、高客单价和Genimo

- 在父体候选子体的非空标题中，完整单词`plastic`命中率≥50%暂归PP，其他归高客单价；50%平局与混合标题均显示提示供复核，不另行用销售额改分类。
- 高客单价为不命中plastic的补集业务名称，不能当作实际价格必然较高的证明。
- Genimo来自同一父体池，按有效品牌中Genimo多数识别。原始品牌与标题保留供复核。
- 整体父体数、父体平均Q列月销量、父体平均T列月销售额分别等于PP+高客单价。按聚合字段核对；不可将均价或增长率直接相加。子体销量和子体销售额仅作为诊断指标，不替代Q/T主指标。
- Genimo整体份额分母为整体市场同指标；Genimo PP份额的分子为Genimo且PP父体，分母为全部PP父体。有效金额份额同时展示两侧覆盖率。

## 7. 时间与BSR分层

- MOM=本月÷去年同月−1，即本月与上一年度同月份比较；YOY=本年共同覆盖月份汇总÷上一年对应共同覆盖月份汇总−1，即本年与上一年比较。表头必须写清中文基期；原表增长率不覆盖本报告重算值。
- 本期或基期无有效值、基期为0，增长率留空。无记录不当作0销售。
- 年度YOY必须使用相同月份集合。当前2025对2024仅比较8—12月；2026对2025仅比较1—7月；2024无基期。列示双方月份、数值和覆盖率，部分年度不得标“全年同比”。月度MOM不能使用上一自然月。
- BSR是独立源行支线：对每条候选源行的有效1—100排名直接划档，不按父ASIN归并，不计算父体平均BSR，也不从92页回取父体指标。
- BSR三档为：头部`(0,20]`、中部`(20,50]`、尾部`(50,100]`。多值源行按其各个有效排名进入对应档位；同一源行可以在多个档位留下BSR观察，但不能据此把行复制回父体主汇总。
- 每个BSR档位、每个月的销量和销售额直接对该档位入档源行的有效Q/T取平均：`BSR档位销量=AVG(该档位源行Q)`，`BSR档位销售额=AVG(该档位源行T)`。BSR支线的销量和销售额不与整体、PP、高客单价做加总勾稽，也不展示父体数量列。
- 候选筛选和BSR分层都不得依赖`Outdoor Rugs`文本；没有有效1—100排名的源行不能进入BSR支线。

## 8. Excel结构与编辑边界

16个工作表：

| 工作表 | 用途 |
| --- | --- |
| 00_数据总览 | 批次、规则摘要、工作表跳转、人工核查入口 |
| 01_整体市场月度、02_PP市场月度、03_高客单价月度 | 24个月指标、均价、MOM/YOY、覆盖率与冲突、分开单位的图表 |
| 04_Genimo品牌月度、04B_Genimo_PP月度 | Genimo整体与PP中的同框架分析 |
| 05_年度YOY | 共同月份、双方分子分母、同比与覆盖率 |
| 06_BSR分层 | 整体、PP、高客单价、Genimo及Genimo PP的三档源行平均指标、MOM/YOY和覆盖诊断 |
| 07_父体冲突诊断 | 所有多值父体的回查 |
| 08_2027策略 | 数据依据、份额比较、建议和使用条件 |
| 09_勾稽检查 | PP+高客单价回加、固定指标列、BSR三档独立平均和Genimo份额 |
| 90_原始输入 | 原始选定字段及来源，只读快照；小类目只回勾，不作为筛选条件 |
| 91_BSR候选子体明细 | 按小类BSR 1—100筛选后的清洗数值、分类标记、有效性、源销量频次、隐含价诊断 |
| 92_父体月度汇总 | 父体Q/T平均销量与销售额、源值诊断、子体覆盖和分类公式；不含BSR支线指标 |
| 93_来源登记 | 文件、月份、Sheet、行数、排除原因和SHA-256 |
| 94_规则说明 | Q/T平均、BSR源行三档平均、固定指标列、缺失值、分类和人工核验路径 |

业务计算保留Excel公式：91有效性/多值诊断→92父体Q/T平均、子体诊断指标和分类→月度→共同月份年度；91的BSR排名另行进入06支线，按源行排名划三档并对各档有效Q/T直接求平均。BSR支线不读取或改写92的父体指标，也不回读勾稽检查结果作为业务输入。

91页数值修改可触发工作簿公式重算。90为原始证据快照，结构、月份、父体键、候选归属或标题品牌的变更须重新构建；不宣称Excel可任意增删行后自动重建候选池。HTML/JSON/Markdown为同批快照，不自动跟随Excel编辑。

## 9. HTML及2027分析

保留原UI风格、数据总览、四部分下拉导航、1.1—4.7定位、折叠与无序列表。提供全部24个月数据、年份过滤、父体月份选择和ASIN检索。销量与金额图分开单位，MOM/三档BSR/覆盖率为交互折线图；空白不绘制为0、不跨缺失值连接曲线。Genimo增加整体/PP份额及2027建议。页面口径必须显示“小类BSR 1—100，不按小类目名称筛选；MOM为去年同月，YOY为上一年度”。

2027建议须引用实际趋势、份额、覆盖率与分层。源数据没有预算、转化或资源约束，不编造固定新增链接数。花型、颜色、尺寸尚未经过完整分类核查时，不编造结论。

页面提供本批次公式Excel下载。HTML内嵌report-data必须与交付JSON完全一致。

## 10. 验证与发布

- 独立重新读取24个原始文件，核对小类BSR排名与候选数量、父体分组及Q/T平均结果；源SHA-256未改变。小类目字段不得作为入池条件。
- 测试BSR1/100、无效排名、多值小类BSR、无小类目文本但有合格BSR、Q/T平均的空值/零值/冲突值、BSR三档源行平均、BSR不做父体归并、固定指标列、去年同月MOM和共同月份年度YOY。
- 对保存的Excel公式独立求值并对比缓存与模型，不能只检查公式存在或缓存一致。用内存副本检验子体金额变更、全部缺失、明确零。
- HTML、JSON、XLSX月份、父体及聚合数字一致；PP+高客单价回加一致；BSR三档只核对各自源行筛选、有效Q/T平均和覆盖率，不与整体市场回加。
- 单独记录工作簿视觉预览、网页交互、Git提交、远端分支及Pages线上内容回读。独立公式求值不等同于桌面Excel整簿重算。
- 本次用户已授权推送网页：完成检验后提交当前业务分支并同步gh-pages，不再要求重复确认。
